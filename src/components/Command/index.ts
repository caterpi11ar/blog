// 导出类型定义（供其他组件使用）
export type { Command, CommandDialogProps, CommandDialogRef } from './CommandDialog'

// 导出主要组件
export { default as FloatingCommand } from './FloatingCommand'
export type { FloatingCommandProps, FloatingCommandRef } from './FloatingCommand'
