---
title: "103. 实战：酷家乐装修编辑器（七）"
author: caterpi11ar
pubDatetime: 2026-04-29T00:00:00
featured: false
draft: false
tags: ["Three.js"]
description: "上节把窗户绘制出来了："
---
上节把窗户绘制出来了：

![2025-06-18 17.50.47.gif](/assets/threejs/d58416bf432b7b9f0a0526d86420a09bb6dd574d.gif)

这节继续来做门的绘制。

和窗户流程是一样的，我们来快速实现一遍：


![image.png](/assets/threejs/f0ccdb9fa787dee13ec20fc56c0804804d160ee2.png)

首先在最后一面墙加一个 doors 的数组，结构和 windows 一样。

```javascript
doors: [
    {
        leftBottomPosition: {
            x: 200,
            z: 20
        },
        width: 300,
        height: 400
    }
]
```
然后在 ts 类型里也加一下：


![image.png](/assets/threejs/f1c14fd956950d4862055a5bb5bee391105de7d9.png)

```javascript
doors?: [
    {
        leftBottomPosition: {
            x: number,
            z: number
        },
        width: number,
        height: number
    }
]
```
绘制的时候也和 windows 差不多。

我们先把 holes 绘制出来：


![image.png](/assets/threejs/e71ef5f9107c0e78b20f318c56209600cc065c76.png)

```javascript
item.doors?.forEach(async door => {
    const path = new THREE.Path();

    const { x, z } = door.leftBottomPosition;

    path.moveTo(x, z);
    path.lineTo(x + door.width, z);
    path.lineTo(x + door.width, z + door.height);
    path.lineTo(x, z + door.height);
    path.lineTo(x, z);
    shape.holes.push(path);
})
```


![image.png](/assets/threejs/084ffb9f82eade5fa11a0a91436a09c8f19beeda.png)

然后找个门的模型。

从 sketchfab.com 来找：

https://sketchfab.com/3d-models/door-adf292f437f24151918a3b16ecef52d2#download


![image.png](/assets/threejs/3a31aeaf314a5448fb2f38a2c5ef76984994fd0a.png)


![image.png](/assets/threejs/d293e0d29243005ab4fc0873424fa250f7c42d42.png)

下载下来放到 public 目录。

写个 loadDoor 方法，和 loadWindow 一样：


![image.png](/assets/threejs/e5852e751f11010b0da82b16b684ee235787a27f.png)

```javascript
let doorModel: { model: THREE.Group, size: THREE.Vector3} | null = null;

async function loadDoor() {
    if(doorModel !== null) {
        return doorModel;
    } else {
        const group = new THREE.Group();
        const loader = new GLTFLoader();
        const gltf = await loader.loadAsync("./door.glb");
        group.add(gltf.scene);
        
        const box = new THREE.Box3();
        box.expandByObject(gltf.scene);

        const size = box.getSize(new THREE.Vector3());
        // console.log('size', size)
        doorModel =  {
            model: group,
            size
        };
        return doorModel;
    }
}
```
渲染出来看一下：

![image.png](/assets/threejs/2e70f91637c7aa4ad1ded078873964e050cc77ec.png)

```javascript
const { model, size} = await loadDoor();
wall.add(model);
```

![image.png](/assets/threejs/e4c9049b3bbb442f3a36c070a21eea3accdfdf6c.png)

调整下大小：

![image.png](/assets/threejs/30d8c7182298ed3b105b27e5797d4bde5e46f261.png)

门的方向和之前不一样，这次调整 y、z 的 scale，然后绕 y 轴旋转 90 度

```javascript
model.scale.y = door.height / size.y;
model.scale.z = door.width / size.z;
model.rotateY(Math.PI / 2);
```

![image.png](/assets/threejs/edbb18bc8a0bb585a41ddf0add22e0c33071575d.png)

现在的大小和角度就都对了。

然后移动下位置：

怎么计算门的位置呢？


![image.png](/assets/threejs/7375a01fc057e33926eb6dc26d0c635b534c404a.png)

leftBottomPosition 加上 width 的一半、height 的一半就可以得到。

试一下：


![image.png](/assets/threejs/3288bf9db934615f27a89afcfd5638f2f55290a6.png)

```javascript
model.position.x = door.leftBottomPosition.x + door.width / 2;
model.position.y = door.leftBottomPosition.z + door.height / 2;
```
![image.png](/assets/threejs/ef39fe13c5c970952913e6da6545b23e7ae67a28.png)

可以看到，计算出来的位置是对的。

此外，之前窗户的位置计算有点问题：


![image.png](/assets/threejs/b38b25a458867000823cae8393315034936e3cb0.png)

之前是窗户正好在中间，所以 x、y 设置墙宽高的一半就行。

实际上也应该是上面的计算方式，改一下：


![image.png](/assets/threejs/6405315d877b0a8d4db2d1a4b3042417fdcd1405.png)

```javascript
model.position.x = win.leftBottomPosition.x + win.width / 2;
model.position.y = win.leftBottomPosition.z + win.height / 2;
```
然后改下数据，让窗户不在正中间：


![image.png](/assets/threejs/a97e30d2d7ad9953fded9ee86d90af4a74e929f8.png)

```javascript
{
    leftBottomPosition: {
        x: 100,
        z: 50
    },
    width: 300,
    height: 300
}
```


![2025-06-18 18.45.55.gif](/assets/threejs/be463b768d3fbdd1be0ad3ce889a99a110ac7774.gif)

窗户的位置依然计算正确。

现在还有一个问题，leftBottomPosition 的 x、z 不好理解，其实它的含义是洞的位置与墙的 left、bottom 的距离。

所以我们统一优化下，改成 left、bottom：


![image.png](/assets/threejs/910c3be82e0c8b049274c5f7ad20d662574891fe.png)


![image.png](/assets/threejs/310b8f9332667f7548de39f1bf9056c77ece0a57.png)


![image.png](/assets/threejs/d7ed2ecb08d331087399c5c625fb95a0b32cfbf6.png)

在 Main/index.tsx 里统一替换 leftBottomPosition.x 为 leftBottomPosition.left

![image.png](/assets/threejs/ed9666696d2a0abfc8abded099034340338ff272.png)

统一替换 leftBottomPosition.x 为 leftBottomPosition.bottom

![image.png](/assets/threejs/2bf06d647774db58f5d212cf87d7096c047f96bc.png)

然后还要改这两个位置：


![image.png](/assets/threejs/88cef47eda257a0f229301f0c040bcf3857eea45.png)


![image.png](/assets/threejs/cc976824201e7f5b42e1678928d5995832c2fd74.png)
```javascript
const { left, bottom } = win.leftBottomPosition;

path.moveTo(left, bottom);
path.lineTo(left + win.width, bottom);
path.lineTo(left + win.width, bottom + win.height);
path.lineTo(left, bottom + win.height);
path.lineTo(left, bottom);
shape.holes.push(path);
```
```javascript
const { left, bottom } = door.leftBottomPosition;

path.moveTo(left, bottom);
path.lineTo(left + door.width, bottom);
path.lineTo(left + door.width, bottom + door.height);
path.lineTo(left, bottom + door.height);
path.lineTo(left, bottom);
shape.holes.push(path);
```
运行效果不变：


![2025-06-18 18.54.45.gif](/assets/threejs/5b1213c308b1329c88920af6dc10be898615f6c3.gif)

但代码可读性提升了，这次重构就算是有意义的。

最后，之前的数组的 ts 类型写的有点问题：


![image.png](/assets/threejs/5106bf5a1e9477c4191fd05d20251a83ed0edcbc.png)

这样写表示数组只有一个元素。

应该这么写：

![image.png](/assets/threejs/355a7422518516d29081250a134cde4bc26b9b80.png)

```javascript
windows: Array<{
    leftBottomPosition: {
        left: number,
        bottom: number
    },
    width: number,
    height: number
}>,
doors?: Array<{
    leftBottomPosition: {
        left: number,
        bottom: number
    },
    width: number,
    height: number
}>
```
并且 windows 加上可选标记 ?

用的时候也做下空值的处理：

![image.png](/assets/threejs/aea5dfd2493878fa9cc0f67f439062c2549ad375.png)

>案例代码上传了[小册仓库](https://github.com/QuarkGluonPlasma/threejs-course-code/tree/main/home-decoration-editor)

## 总结

这节我们加上了门的数据结构以及绘制逻辑，并且重构了下代码。

门的数据结构和窗户的一样，都是记录洞的位置，然后绘制的时候把它用 Shape 画出来，加到 holes 数组里，之后加载一个门的模型放在那个位置。

只不过因为旋转角度不同，scale 需要设置不同方向的值。

门窗绘制好了，下节来绘制地板、天花板。
