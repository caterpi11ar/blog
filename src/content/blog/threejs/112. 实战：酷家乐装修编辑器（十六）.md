---
title: "112. 实战：酷家乐装修编辑器（十六）"
author: caterpi11ar
pubDatetime: 2026-04-29T00:00:00
featured: false
draft: false
tags: ["Three.js"]
description: "上节实现了尺寸标注："
---
上节实现了尺寸标注：

![2025-06-27 18.04.44.gif](/assets/threejs/84c9b5fd9aba94b0a505d72794ee33bfd8d2f00f.gif)

这节我们开始做户型切换、家具的导入和编辑。

![image.png](/assets/threejs/2d3f316443d96d9ea3355e206cc17f793230bee8.png)

我们把左侧的菜单分为两个列表，一个是户型列表，可以切换户型，一个是家具列表，可以导入家具。

用 antd 的 [Segmented](https://ant.design/components/segmented-cn) 组件：

![image.png](/assets/threejs/d2cedb46d77a867b84a8454f9342930cdc5871b2.png)

改下 components/Menu/index.tsx

![image.png](/assets/threejs/5f3bee4e1d2ad27f88081109a95250a7cc10201d.png)

Segmented 的options 里是这两个选项对应的 label 和值。

用一个状态保存 key，Segmented 的onChange 里 setKey

当 key 为户型和家具的时候，分别展示不同的内容。

这里还用到了 @ant-design/icons 的 icon

安装下：

```
npm install --save @ant-design/icons
```
```javascript
import { HomeOutlined, UngroupOutlined } from "@ant-design/icons";
import { Segmented } from "antd";
import { useState } from "react";

function Menu() {

    const [left, setLeft] = useState(0);

    const [key, setKey] = useState('户型');

    return <div className="Menu" style={{left: left}}>
        <Segmented value={key} onChange={setKey} block options={[
            {
                label: <div>
                    <HomeOutlined />
                    <span style={{padding: 10}}>户型</span>
                </div>,
                value: '户型',
            },
            {
                label: <div>
                    <UngroupOutlined />
                    <span style={{padding: 10}}>家具</span>
                </div>,
                value: '家具',
            },
        ]} />
        {
            key === '户型' ? <div>
                111
            </div> : null
        }
        { key === '家具' ? <div>
                222
            </div> : null
        }
        <div className="drawer-bar" onClick={() => {
            setLeft(left === 0 ? -300 : 0);
        }}></div>
    </div>
}

export default Menu;
```

顺便把样式里的背景色改成白色：

![image.png](/assets/threejs/9bfbb4ee034861b586c0d3ecc43f1346d58aa38d.png)

看下效果：

![2025-06-27 21.13.09.gif](/assets/threejs/8d81e63b6979996ce53e0a45de02622bb1d5e12f.gif)

然后写一下户型列表：

现在只有两个户型：


![house1.png](/assets/threejs/7278d7f3ed3572a4c8e378ea720fc8b08bed11fb.png)

![house2.png](/assets/threejs/c37a14c77ee7f37db7854c775cf06e042cc2e8f1.png)

把这两张图放到 public 目录下：

![image.png](/assets/threejs/29213545fb6b9f52662a14dda6eb3d8fdeb6c895.png)

然后写一下家具列表：

用 antd 的 Card 组件：


![image.png](/assets/threejs/14903dcef887854298356ba487e1d6da9b4e1322.png)

```javascript
import { Card, Image, Segmented } from "antd";
```
```javascript
{
    key === '户型' ? <div>
        <Card
            hoverable
            style={{ width: 200, margin: 20 }}
            cover={<Image
                width={200}
                src="./house1.png"
            />}
        >
            <Meta title="1室1厅0厨0卫" description="" />
        </Card>
        <Card
            hoverable
            style={{ width: 200, margin: 20 }}
            cover={<Image
                width={200}
                src="./house2.png"
            />}
        >
            <Meta title="1室2厅0厨0卫" description="" />
        </Card>
    </div> : null
}
```

![2025-06-27 21.25.51.gif](/assets/threejs/297d6b840e40aab32f513e6f553583f3fba5c4b7.gif)

然后点击的时候清空当前户型数据，设置为新的户型。

酷家乐里也是这样：

![2025-06-27 21.27.09.gif](/assets/threejs/7f68d3c91bcee0a11ba00f8ce91a718ea9fc265f.gif)

切换户型会清空当前数据。

我们给 store 添加一个方法：

![image.png](/assets/threejs/134519bd799963be8f1c5ed667f03a402ebed5ac.png)

这里要添加一个 Action 类型，然后 setData 里返回新的 state。

```javascript
export interface Action {
    setData(data: State['data']): void;
}

const useHouseStore = create<State & Action>((set, get) => {
    return {
        data: data,
        setData(data) {
            set(state => {
                return {
                    ...state,
                    data: data
                }
            })
        }
    }
});
```
在 Menu 组件里点击户型的时候调用 setData：


![image.png](/assets/threejs/2d6a49d0e39ac9e2a90cfd01b93dddfc17490cb7.png)


![image.png](/assets/threejs/117d4d6614b71a3f1fad5e21c34a1ad02347c704.png)

```javascript
import data1 from "../../store/house1";
import data2 from "../../store/house2";
```
```javascript
const {setData} = useHouseStore();
```
```javascript
onClick={() => setData(data1)}
```
```javascript
onClick={() => setData(data2)}
```

试下效果：

![2025-06-27 21.40.24.gif](/assets/threejs/ea582af8e6d4110f820776d13f5b510e02ffa68e.gif)

新的户型数据确实设置好了，但之前的场景没清空。

我们处理下：


![image.png](/assets/threejs/c41e7a1e8859bbc5c5e0c6ad0faebdba71e87fb0.png)


![image.png](/assets/threejs/e68debccf96fec71eb2cc209791be10c4dc8bf1a.png)

```javascript
house.name = 'house';
```
给两个场景的 hosue 加个 name。

然后 data 变化的时候把 house 删掉：


![image.png](/assets/threejs/dc6095be630db3053e9876f0564e275c793e3f5a.png)

```javascript
useEffect(() => {
    const scene1 = scene2DRef.current;
    const scene2 = scene3DRef.current;
    const house1 = scene1?.getObjectByName('house');
    const house2 = scene2?.getObjectByName('house');

    house1?.parent?.remove(house1);
    house2?.parent?.remove(house2);

    house1?.traverse(item => {
        let obj = item as THREE.Mesh;
        if(obj.isMesh) {
            obj.geometry.dispose();
        }
    })
}, [data])
```

根据 name 查找到 house，把它删掉，并且遍历释放所有 geometry 的 gpu 资源。

试下效果：


![2025-06-27 23.12.33.gif](/assets/threejs/e80ed4a46fff60d69a4d71c328e058dfa3c70db3.gif)

有一个问题，重新渲染之后，模型不会重新加载：

![image.png](/assets/threejs/badb6a13489a203116cb1ca5e6b88f0e1c6b02c2.png)

去掉之前的缓存逻辑，每次重新加载就好了：

![image.png](/assets/threejs/84a27b4dd7b36494ffb178a3009f9f5abbb4fbe7.png)


![image.png](/assets/threejs/238f732e15cb7409e2b5de130bebf632784c1a6f.png)

```javascript
async function loadWindow() {
    const group = new THREE.Group();
    const loader = new GLTFLoader();
    const gltf = await loader.loadAsync("./window.glb");
    group.add(gltf.scene);
    
    const box = new THREE.Box3();
    box.expandByObject(gltf.scene);

    const size = box.getSize(new THREE.Vector3());
    return {
        model: group,
        size
    };
}

async function loadDoor() {
    const group = new THREE.Group();
    const loader = new GLTFLoader();
    const gltf = await loader.loadAsync("./door.glb");
    group.add(gltf.scene);
    
    const box = new THREE.Box3();
    box.expandByObject(gltf.scene);

    const size = box.getSize(new THREE.Vector3());
    return {
        model: group,
        size
    }
}

```

![2025-06-27 23.18.25.gif](/assets/threejs/8a27c63993663e8918c4ec82a9dc81ae886f6b3f.gif)

最后我们还要加一个提醒：


![image.png](/assets/threejs/48d5bd6321ad28874178137b3a7f333769130b33.png)


![image.png](/assets/threejs/34b70c4b3f4ac3f3c24753ed4a3d3a6da945b358.png)

用 antd 的 PopConfirm 组件来实现。

```javascript
{
    key === '户型' ? <div>
        <Popconfirm
            title="提醒"
            description="切换户型将清空数据，是否继续？"
            onConfirm={() => setData(data1)}
            okText="是"
            cancelText="否"
        >
            <Card
                hoverable
                style={{ width: 200, margin: 20 }}
                cover={<Image
                    width={200}
                    src="./house1.png"
                />}
            >
                <Meta title="1室1厅0厨0卫" description="" />
            </Card>
        </Popconfirm>

        <Popconfirm
            title="提醒"
            description="切换户型将清空数据，是否继续？"
            onConfirm={() => setData(data2)}
            okText="是"
            cancelText="否"
        >
            <Card
                hoverable
                style={{ width: 200, margin: 20 }}
                cover={<Image
                    width={200}
                    src="./house2.png"
                />}
            >
                <Meta title="1室2厅0厨0卫" description="" />
            </Card>
        </Popconfirm>
    </div> : null
}
```
试一下：

![2025-06-27 23.22.54.gif](/assets/threejs/494724b8c0b9897f92807ff3f809fa86a9523127.gif)

这样，户型列表和切换就做好了。

最后我们解决下这个的位置问题：


![image.png](/assets/threejs/dad5a9a4b8d8423d4cf9328df25d1f0c0ee7f66d.png)

把 left 改为 320px：

![image.png](/assets/threejs/eac5f718f78e9b249ff205bb889aa21e0152ccb6.png)


试一下：


![2025-06-27 23.22.54.gif](/assets/threejs/e15975c776533e8fb3da6d6e52e532b1c560e756.gif)

>案例代码上传了[小册仓库](https://github.com/QuarkGluonPlasma/threejs-course-code/tree/main/home-decoration-editor)


## 总结

这节我们实现了户型切换。

首先我们用 Segmented、Card 组件实现了户型列表的布局，点击的时候修改 store 里的数据来实现户型切换。

需要注意的是切换户型的时候，要找到之前 house 对象，把它删掉。

户型列表和切换实现后，下节就可以来做家具列表和家具导入了。