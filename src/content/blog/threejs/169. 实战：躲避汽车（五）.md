---
title: "169. 实战：躲避汽车（五）"
author: caterpi11ar
pubDatetime: 2026-04-29T00:00:00
featured: false
draft: false
tags: ["Three.js"]
description: "上节实现了碰撞的流血效果："
---
上节实现了碰撞的流血效果：

![2025-06-03 23.16.28.gif](/assets/threejs/197886350b5f4c0db993e7d8ed2c00aaf5c38d10.gif)

这节我们加一些文字，比如开始的 Ready Go，结束时的 Game Over，以及左右箭头。

这个用 SpriteText 来做。

安装下：

```javascript
npm install --save three-spritetext
```
创建 text.js

```javascript
import SpriteText from "three-spritetext";

export function createLeftArrow() {
    const arrowLeft = new SpriteText('←', 200);
    arrowLeft.color = 'white';
    arrowLeft.strokeColor = 'blue';
    arrowLeft.strokeWidth = 1;
    arrowLeft.position.x = -500;
    arrowLeft.position.y = 100;
    arrowLeft.position.z = 100;
    return arrowLeft;
}
```
我们先画一下左箭头。

字体是白色，有蓝色的描边。

在 main.js 里引入：

![image.png](/assets/threejs/c5e07f305d7fc5a589dd4bfc8912eb30d184233c.png)

```javascript
scene.add(createLeftArrow());
```


![image.png](/assets/threejs/57b1e76b449bc65f2c28d1505fc16daeba700c63.png)

然后添加右箭头：

```javascript
export function createRightArrow() {
    const arrowLeft = new SpriteText('→', 200);
    arrowLeft.color = 'white';
    arrowLeft.strokeColor = 'blue';
    arrowLeft.strokeWidth = 1;
    arrowLeft.position.x = 500;
    arrowLeft.position.y = 100;
    arrowLeft.position.z = 100;
    return arrowLeft;
}
```
```javascript
scene.add(createRightArrow());
```

![image.png](/assets/threejs/bd835ffdbde34016b2bd3206b036c834f1fc5208.png)

接下来是最开始的 Ready Go

```javascript
export function createReady() {
    const ready = new SpriteText('Ready', 200);
    ready.color = 'white';
    ready.strokeColor = 'lightgreen';
    ready.strokeWidth = 1;

    ready.position.y = 200;
    ready.position.z = 100;
    return ready;
}
```
```javascript
scene.add(createReady());
```

![image.png](/assets/threejs/5846234a9eef848239b104d6d0c7e0eb15a8065e.png)

直接出现太突兀了，我们用 gsap 做一下从远到近的补间动画。


![image.png](/assets/threejs/9541240a0bc7dff0c2d4edd1f3b7c06bb1c4a9f5.png)

设置 z 和 textHeight 的初始值。

用 timeline 来做并行的 z 和 textHeight 变化的动画。

0.5s 后串行做 textHeight 变小的动画，这个速度要快一点。

```javascript
export function createReady() {
    const ready = new SpriteText('Ready', 200);
    ready.color = 'white';
    ready.strokeColor = 'green';
    ready.strokeWidth = 1;

    ready.position.y = 200;
    
    ready.position.z = -500;
    ready.textHeight = 0;

    const timeline = gsap.timeline();
    timeline
        .to(ready.position, {
            duration: 0.5,
            z: 100,
        })
        .to(ready, {
            textHeight: 200,
            duration: 0.5
        }, '<')
        .to(ready, {
            textHeight: 0,
            duration: 0.05,
            onComplete: () => {
                ready.parent.remove(ready);
            }
        }, '+=0.5');
    return ready;
}
```

![2025-06-04 08.35.55.gif](/assets/threejs/8ca344a21eaef53ad1c7c7351f34828e38a3719f.gif)

没啥问题，我们再同样的方式写下 Go 的

```javascript
export function createGo() {
    const ready = new SpriteText('Go', 200);
    ready.color = 'white';
    ready.strokeColor = 'green';
    ready.strokeWidth = 1;

    ready.position.y = 200;
    
    ready.position.z = -500;
    ready.textHeight = 0;

    const timeline = gsap.timeline();
    timeline
        .to(ready.position, {
            duration: 0.5,
            z: 100,
        }, '+=1')
        .to(ready, {
            textHeight: 200,
            duration: 0.5
        }, '<')
        .to(ready, {
            textHeight: 0,
            duration: 0.05,
            onComplete: () => {
                ready.parent.remove(ready);
            }
        }, '+=0.5');
    return ready;
}
```
```javascript
scene.add(createGo());
```
唯一的区别是最开始要延迟 1s


![image.png](/assets/threejs/51eec05764b0701d5ffddd3e38dfc30e9ea4c632.png)


![2025-06-04 08.38.33.gif](/assets/threejs/3264ef31818bf76a232cee46f40122eeb4106446.gif)

最后，我们加上 GameOver

```javascript
export function createGameOver() {
    const ready = new SpriteText('Game Over', 200);
    ready.color = 'red';
    ready.strokeColor = 'white';
    ready.strokeWidth = 1;
    ready.padding = 80;

    ready.position.y = 200;
    
    ready.position.z = -500;
    ready.textHeight = 0;

    const timeline = gsap.timeline();
    timeline
        .to(ready.position, {
            duration: 0.3,
            z: 100,
        })
        .to(ready, {
            textHeight: 120,
            duration: 0.3
        }, '<');
    return ready;
}
```
在碰撞的时候再展示：


![image.png](/assets/threejs/d5a0b1cb605c505943f90b8bbe190b87880fed80.png)

```javascript
scene.add(createGameOver());
```
看下效果：


![2025-06-04 08.43.55.gif](/assets/threejs/277fe839096e733ef6583c713a39f2252357a177.gif)

没啥问题。

最后我们来优化一下：

现在走路是 300ms 一次，改成 200ms，这样灵敏一些。


![image.png](/assets/threejs/115cd6cc25f3f3e0944ddf23833be6d9a4ab7904.png)

去掉 OrbitControls，玩家不需要改变相机位置。

![image.png](/assets/threejs/b9afa8163c9f6d05fb7872f65c5f803aa5849164.png)

然后最开始，我们做一个从远到近的相机动画：


![image.png](/assets/threejs/e5597c33160a76024ca0142a966e2b49b4658291.png)

```javascript
camera.position.y = 2000;
camera.position.z = 2000;
gsap.to(camera.position, {
  y: 500,
  z: 500,
  duration: 2
});
```
2s 内镜头从远到近。


![2025-06-04 08.50.52.gif](/assets/threejs/ca37db3b33f81cb022efdca9f4455f0f24a8396a.gif)

这样，入场效果更好了。

还有两个小问题要修复：

一个是现在车的速度是是随机的 10 到 15


![image.png](/assets/threejs/38a430cf0a5d28483bc9c90d9042ec7da3a2e0ff.png)

这样同一车道的车是有追尾的可能的。

应该是中间两条车道速度快，两边速度慢，但同一车道的车速度一样。

![image.png](/assets/threejs/95c552ca0dfd67d89629397fba8fa53b3489654e.png)

```javascript
car.speed = [1, 2].includes(index) ? 20 : 10
```

![2025-06-04 10.56.54.gif](/assets/threejs/b1c5842df4e1b44407e2c9e3009cebb587ce64e9.gif)

另一个问题是当你切换到其他页面或者其他软件一段时间，再切回来：


![image.png](/assets/threejs/fb9b2f24a6f41fb3df940cfa85fb2c26febce9c0.png)

会一下子有很多的车。

因为定时器没有暂停。

我们要检测下页面可见性，不可见的时候就暂停创建车子：


![image.png](/assets/threejs/fc7dc1acb32ee6c7677e70d3d5ccfce94a8207c1.png)

```javascript
let paused = false;
document.addEventListener('visibilitychange', (e) => {
    paused = document.visibilityState !== 'visible'
});
```
```javascript
if(paused) {
    return;
}
```
这样，游戏体验就没啥问题了。

>案例代码上传了[小册仓库](https://github.com/QuarkGluonPlasma/threejs-course-code/tree/main/dodge-cars)

## 总结

这节我们加上了文字，包括左右箭头、Ready、Go、Game Over

用 three-spritetext 实现的，不用自己用 canvas 画。

并且用 gasp 做了文字放大、消失的补间动画。

最后我们还加了一个镜头从远到近的入场相机动画。

至此，我们的躲避汽车游戏就完成了。
