---
title: "173. 实战：佛光普照（三）"
author: caterpi11ar
pubDatetime: 2026-04-29T00:00:00
featured: false
draft: false
tags: ["Three.js"]
description: "上节画了佛光的粒子："
---
上节画了佛光的粒子：

![2025-08-10 10.43.35.gif](/assets/threejs/f48fc8c2af787426f0f95cfdd9c0b8e9c7f7c8c8.gif)

这节我们把后面的圆环画一下：

![image.png](/assets/threejs/0d04692686b5db8be5b7efcbabc22673663aa462.png)


![image.png](/assets/threejs/0f4b2b3fd33a15ae2e7a09b4fe35d9aeedd4e994.png)

当然，这个没有标准，怎么画都行，我们综合一下。

和我们之前画的云雷纹差不多：

![2025-08-09 23.35.36.gif](/assets/threejs/db6a2973eea6f0f8da2007852de79fa50be69cc0.gif)

这只不过图案不一样。

创建 halo.js

```javascript
import * as THREE from 'three';
import { Line2, LineGeometry, LineMaterial } from 'three/examples/jsm/Addons.js';
import { loadPromise } from './mesh';

const group = new THREE.Group();

const arc1 = new THREE.EllipseCurve(0, 0, 130 , 130, 0, Math.PI * 2);
const pointsArr1 = arc1.getPoints(50);
const geometry1 = new LineGeometry();
geometry1.setFromPoints(pointsArr1);
const material1 = new LineMaterial({
    color: new THREE.Color('gold'),
    linewidth: 10
});
const line1 = new Line2(geometry1, material1);
group.add(line1);

loadPromise.then((gltf) => {
    const box3 = new THREE.Box3();
    box3.expandByObject(gltf.scene);

    const size = box3.getSize(new THREE.Vector3());

    group.position.y = size.y * 2 / 3;
    group.position.z = -size.z / 2;
})

export default group;
```
先画一个大的圆环。

因为要设置 lineWidth，这里得用 LineGEometry、LineMaterial、Line2 的 api

然后等模型加载完，调到佛祖三分之二高的位置。

引入下看效果：


![image.png](/assets/threejs/549e99602a552c8ded4914e7ea7f1f0faa0b108f.png)


![2025-08-10 11.10.18.gif](/assets/threejs/6323240e14e992ec46977378fc0ae96ee4e7b398.gif)

然后再画一圈线：

![image.png](/assets/threejs/b28b6eb5315a1ccb097f68ab01e7aeb70a78a1ac.png)


![image.png](/assets/threejs/0a5428cac9651a47f6554ae3053201c3073890c0.png)

```javascript
const geometry2 = new LineGeometry();
const pointsArr2 =  [];
for(let angle = 0; angle <= Math.PI * 2; angle += Math.PI  / 6) {
    pointsArr2.push(
        new THREE.Vector3(0, 0, 0),
        new THREE.Vector3(150 * Math.cos(angle), 150 * Math.sin(angle), 0),
    )
}
geometry2.setFromPoints(pointsArr2);
const material2 = new LineMaterial({
    color: new THREE.Color('white'),
    linewidth: 2
});
const line2 = new Line2(geometry2, material2);
group.add(line2);
```
这里要设置 LineWidth，所以也是用 Line2


![image.png](/assets/threejs/2d29920cdb08f777498df3a6a85e3744ee1af5e3.png)

然后多加几圈圆圈：



![image.png](/assets/threejs/fc60e17f057273768622e852056f5416187b98aa.png)

```javascript
for(let i = 0; i<= 2; i++) {
    const arc = new THREE.EllipseCurve(0, 0, 110 + i * 20 , 110 + i * 20, 0, Math.PI * 2);
    const pointsArr = arc.getPoints(50);
    const geometry = new LineGeometry();
    geometry.setFromPoints(pointsArr);
    const material = new LineMaterial({
        color: new THREE.Color('gold'),
        linewidth: 3
    });
    const line = new Line2(geometry, material);
    group.add(line);
}
```
110、130、150 三个圆圈，宽度为 3


![image.png](/assets/threejs/0c12920491295ad44b9ed3637538877a408d3083.png)

最后加一个最里面的圆：


![image.png](/assets/threejs/90237105fc1c4cfc2a7059c3ccec2d2f0ab3ed59.png)

```javascript
const circleGeometry = new THREE.CircleGeometry(110);
const circleMaterial = new THREE.MeshBasicMaterial({
    color: '#faeb6c'
});
const circle = new THREE.Mesh(circleGeometry, circleMaterial);
circle.position.z = 1;
group.add(circle);
```

![image.png](/assets/threejs/be9d870c594b71248693dfd77ee5ab2f6595010e.png)

去掉包围盒和 AxesHelper

![image.png](/assets/threejs/a6a796ac60daccab13341ab6afb5b3d1b72222cb.png)

给这个光环加一个转动的动画：

用 gsap：

```
pnpm install --save-dev gsap
```

![image.png](/assets/threejs/62ed1d70fcc1a370bcb110f298e79b485eac0b4c.png)

不断旋转，并且scale 在 1 到 1.2 之间循环往复，yoyo 是悠悠球那种循环往复的动画效果。

```javascript
gsap.to(group.scale, {
    x: 1.2,
    y: 1.2,
    z: 1.2,
    duration: 2,
    repeat: -1,
    yoyo: true,
    ease: 'none'
});

gsap.to(group.rotation, {
    z: Math.PI  * 2,
    duration: 10,
    repeat: -1,
    ease: 'none'
});
```

![2025-08-10 11.52.34.gif](/assets/threejs/cbb96c5fd445ffe41f184280a93bad21612d7bff.gif)

然后加上一圈 卍，不然有点单调

这个要用 SpriteText

```
pnpm install --save three-spritetext
```


![image.png](/assets/threejs/3ed29a2f108a559c9b5cb55f2b665793967c17f6.png)

```javascript
const figureGroup = new THREE.Group();
for(let angle = 0; angle <= Math.PI * 2; angle += Math.PI  / 30) {
    const figureText = new SpriteText('卍', 12);
    figureText.color = 'gold';
    figureText.strokeWidth = 1;
    figureText.strokeColor = 'white';
    figureText.position.set(160 * Math.cos(angle), 160 * Math.sin(angle), 0);
    figureGroup.add(figureText);
}
group.add(figureGroup);
```

再加两圈数量少点的：

![image.png](/assets/threejs/a87f29abb51785ad324228cec4b71831de6cc196.png)

数量、半径不一样，内圈那个开始角度也调一下，不然文字正好在线上

```javascript
for(let angle = 0; angle <= Math.PI * 2; angle += Math.PI  / 15) {
    const figureText = new SpriteText('卍', 12);
    figureText.color = 'gold';
    figureText.strokeWidth = 1;
    figureText.strokeColor = 'white';
    figureText.position.set(140 * Math.cos(angle), 140 * Math.sin(angle), 0);
    figureGroup.add(figureText);
}
for(let angle = Math.PI / 10; angle <= Math.PI * 2; angle += Math.PI  / 6) {
    const figureText = new SpriteText('卍', 12);
    figureText.color = 'gold';
    figureText.strokeWidth = 1;
    figureText.strokeColor = 'white';
    figureText.position.set(120 * Math.cos(angle), 120 * Math.sin(angle), 0);
    figureGroup.add(figureText);
}
```

![2025-08-10 12.06.42.gif](/assets/threejs/1cb435bcf0cc262c7a4f4cc443d2ee7c3f9af7ca.gif)

有点乱，去掉最外面一圈。


![image.png](/assets/threejs/6967b639682d57f56a3d1e0f24fd8d8f186ca3a6.png)


![image.png](/assets/threejs/7a72c14146f7b6b821bc6fb2e0a97757c6d05696.png)

让它做反向运动：

```javascript
gsap.to(figureGroup.rotation, {
    z: -Math.PI  * 2,
    duration: 5,
    repeat: -1,
    ease: 'none'
})
```

![2025-08-10 12.10.30.gif](/assets/threejs/6dc5ff8c9c6a5cee38dfbb8706b101cad53b6cbe.gif)

最后加一个透明度变化的动画，那种渐隐渐现的效果：


![image.png](/assets/threejs/b51d2639ceaa9832a6a004e556f78a8911170f8f.png)

```javascript
let obj = { opacity: 0.2 };
gsap.to(obj, {
    opacity: 1,
    duration: 2,
    repeat: -1,
    yoyo: true,
    ease: 'none',
    onUpdate() {
        group.traverse(o => {
            if(o.isMesh || o.isSprite) {
                o.material.transparent = true;
                o.material.opacity = obj.opacity;
            }
        })
    }
})
```
透明度从 0.2 到 1 变化，遍历所有对象修改透明度。


![2025-08-10 12.10.30.gif](/assets/threejs/848393625ab1c86aa2178e44a42c25d8ae6788c7.gif)

佛光的光线太长了，我们调整下轨迹的时间：


![image.png](/assets/threejs/301b0e13abe67151e5e6479fbda3b45cec84e1fe.png)

大小是 1 到 3，轨迹时长是 1 到 5s，一次 500 个

```javascript
startSize: new IntervalValue(1, 3),
```
```javascript
emissionOverTime: new ConstantValue(500),
```
```javascript
rendererEmitterSettings: {
    startLength: new IntervalValue(1,5)
},
```

![2025-08-10 12.29.01.gif](/assets/threejs/af721cdf697c9fce4590f480198337fe9b7fdd84.gif)

>案例代码上传了[小册仓库](https://github.com/QuarkGluonPlasma/threejs-course-code/tree/main/online-buddha)

## 总结

这节我们加上了外面的光环。

画设置 lineWidth 的线需要用 LineGeometry、LineMaterial、Line2 的 api

我们分别用 Line、Circle、SpriteText 画了一圈圈光环和文字。

最后用 gasp 做了 scale、opacity、rotation 的动画。

下节我们继续来加上底座的部分。
