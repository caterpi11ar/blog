---
title: "198. 实战：小镇旅游（一）"
author: caterpi11ar
pubDatetime: 2026-04-29T00:00:00
featured: false
draft: false
tags: ["Three.js"]
description: "学会了如何控制人物在 3D 场景内漫游，有没有想实现一个元宇宙旅游项目的冲动呢？"
---
学会了如何控制人物在 3D 场景内漫游，有没有想实现一个元宇宙旅游项目的冲动呢？

其实我们去旅游也是去各处景点看一下，上下左右的看看。

如果我们能拿到景点的高清扫描模型，然后控制玩家在里面漫游，是不是就和亲身漫游体验一样呢？

这节开始，我们就来实现一个元宇宙旅游的项目。

我们还没学建模，这里用 sketchfab.com 的一个小镇模型：

![image.png](/assets/threejs/d1eae79d69d6e354b2eab17b3cdaed3c9e9a9de3.png)

还原度很高。

https://sketchfab.com/3d-models/street-town-f6ffeb1103814fd08f093c21575776e5

我们就基于这个模型来实现元宇宙旅游的功能。

创建项目：


```
npx create-vite meta-tourism
```

![image.png](/assets/threejs/c0e3cf028eb1a1e5d3ea192a48511fb18b9bec8b.png)

进入项目，安装依赖：

```
pnpm install
pnpm install --save three
pnpm install --save-dev @types/three
```
先写下基础代码：

改下 src/main.js

```javascript
import './style.css';
import * as THREE from 'three';
import {
    OrbitControls
} from 'three/addons/controls/OrbitControls.js';
import mesh from './mesh.js';

const scene = new THREE.Scene();

scene.add(mesh);

const light = new THREE.DirectionalLight(0xffffff);
light.position.set(500, 300, 600);
scene.add(light);

const light2 = new THREE.AmbientLight();
scene.add(light2);

const axesHelper = new THREE.AxesHelper(1000);
scene.add(axesHelper);

const width = window.innerWidth;
const height = window.innerHeight;

const camera = new THREE.PerspectiveCamera(60, width / height, 1, 10000);
camera.position.set(0, 500, 500);
camera.lookAt(0, 0, 0);

const renderer = new THREE.WebGLRenderer({
  antialias: true
});
renderer.setSize(width, height)

function render() {
    renderer.render(scene, camera);
    requestAnimationFrame(render);
}

render();

document.body.append(renderer.domElement);

const controls = new OrbitControls(camera, renderer.domElement);
```
创建 Scene、Light、Camera、Renderer

改下 style.css
```css
body {
    margin: 0;
}
```
然后创建 mesh.js

```javascript
import * as THREE from 'three';

const geometry = new THREE.BoxGeometry(100, 100, 100);
const material = new THREE.MeshPhongMaterial({
    color: 'orange'
})
const mesh = new THREE.Mesh(geometry, material);

export default mesh;
```
先画个立方体

跑一下：

```
npm run dev
```

![image.png](/assets/threejs/8fe156722edad88f92ecf29869a47b8480ba1258.png)


![2025-09-22 12.45.27.gif](/assets/threejs/b10b8b1c2748546da0ada3156d1b8ec6c4606a44.gif)

把小镇模型下载下来：

![image.png](/assets/threejs/4d992508e5545871eb1258595f69f24c523e418c.png)

![image.png](/assets/threejs/c3f1efdc074d8ac4f4759e244b7b5be031057db5.png)

在场景里加载下：


```javascript
import * as THREE from 'three';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';

const loader = new GLTFLoader();

const mesh = new THREE.Group();

loader.load("./town.glb", function (gltf) {
    console.log(gltf);
    gltf.scene.scale.setScalar(20);
    mesh.add(gltf.scene);
})

export default mesh;
```
看下效果：

![2025-09-22 16.19.21.gif](/assets/threejs/bff65235b63c6bed2eb0942c0efea326d89c8752.gif)

街道有点短，我们可以复制几个拼接起来：

先看下包围盒大小：


![image.png](/assets/threejs/b63c1e7bbc35ef330353854bd69deeb90f8d042f.png)

```javascript
const helper = new THREE.BoxHelper(gltf.scene);
mesh.add(helper);
```

![2025-09-22 16.29.35.gif](/assets/threejs/ab78934b15acbe270732b7e20d64a46f0e9afb69.gif)

比实际略大一些，我们获取到包围盒大小之后，需要减掉一些。


![image.png](/assets/threejs/077c8c5363d133234f552c3a9eade516e2a94eac.png)

```javascript
const box = new THREE.Box3();
box.expandByObject(gltf.scene);

const size = box.getSize(new THREE.Vector3());

for(let i = 1; i <= 4; i++) {
    const newTown = gltf.scene.clone();
    mesh.add(newTown);
    newTown.position.x  = (size.x -  350) * i;
}
```

看下效果：


![image.png](/assets/threejs/83e3d8369d58aa4bf342bde54b92c4c2c7a433d5.png)

另一侧再连上 5 个：


![image.png](/assets/threejs/99ab90377178fdb3a39ec35e4dae416dde40d504.png)

```javascript
for(let i = 1; i <= 4; i++) {
    const newTown = gltf.scene.clone();
    mesh.add(newTown);
    newTown.position.x  = (size.x -  330) * i;
}
for(let i = 0; i < 5; i++) {
    const newTown = gltf.scene.clone();
    mesh.add(newTown);
    newTown.position.x  = (size.x -  350) * i;
    newTown.position.z  = size.z - 470;
    newTown.rotation.y = Math.PI;
}
```
这里旋转、位置，慢慢调一下就行。

看下效果：

![image.png](/assets/threejs/19e1daa80ece6369363e01e07026e584480dbd52.png)

这样，小镇就初具规模了。

>案例代码上传了[小册仓库](https://github.com/QuarkGluonPlasma/threejs-course-code/tree/main/meta-tourism)

## 总结

这节我们开始做元宇宙旅游的项目。

我们先搭了一个小镇出来。

通过一个小镇模块复制了几次连接起来。

下节加入玩家。
