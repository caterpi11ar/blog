---
title: "259. 全屏滚动官网（三）"
author: caterpi11ar
pubDatetime: 2026-04-29T00:00:00
featured: false
draft: false
tags: ["Three.js"]
description: "上节加上了切换到某一屏时的动画，以及鼠标交互："
---
上节加上了切换到某一屏时的动画，以及鼠标交互：

![2025-09-03 19.05.32.gif](/assets/threejs/ad3e179dea60812adfae17d5a452088b92c2c5b9.gif)

但现在的 3D 场景的物体有点简单，我们只是控制它的放缩、位移这些。

如果是想通过滚动控制复杂的动画呢？

比如滚动控制跳舞的骨骼动画的进度？

当然也是可以的。

我们找个带骨骼动画的模型：

https://sketchfab.com/3d-models/fish-5a2b7976ef5c44658de22e98bf381277#download


![2025-09-05 11.47.31.gif](/assets/threejs/8772657c3aa074acdeaf9812a78965d437891669.gif)

比如这些鱼的尾巴摆动的骨骼动画

就可以通过滚动来控制进度。

下载下来放到 public 目录：


![image.png](/assets/threejs/2b8953b65e44eff71f5f7e4ce8e9d72e6f02f019.png)

再加一屏：


![image.png](/assets/threejs/e144c4e0bb53d595ff8d7f95f06eed81ce771753.png)

```html
<div className='section section4'>
    <h1>鱼儿游啊游</h1>
</div>
```


![2025-09-05 11.51.49.gif](/assets/threejs/a9394e31a4246b67af793fda39b1f0e246f6a48f.gif)

然后把模型加载进来：

创建 fish.js

```javascript
import gsap from 'gsap';
import * as THREE from 'three';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';

const loader = new GLTFLoader();

const mesh = new THREE.Group();

loader.load("./fish.glb", function (gltf) {
    console.log(gltf);
    mesh.add(gltf.scene);

    gltf.scene.scale.setScalar(50);

    const mixer = new THREE.AnimationMixer(gltf.scene);
    const clipAction = mixer.clipAction(gltf.animations[0]);
    clipAction.play();

    const clock = new THREE.Clock();
    function render() {
        requestAnimationFrame(render);

        const delta = clock.getDelta();
        mixer.update(delta);
    }
    render();

    const fish1 = gltf.scene.getObjectByName("BrownFishArmature_13");
    const fish2 = gltf.scene.getObjectByName("ClownFishArmature_23");
    const fish3 = gltf.scene.getObjectByName("TunaArmature_33");
    const fish4 = gltf.scene.getObjectByName("DoryArmature_47");

    fish1.parent.remove(fish1, fish3, fish4);

    fish2.name = 'fish';

})

export default mesh;
```
加载模型，播放播放动画。

这里有 4 条鱼，删掉 3 条留下 1 条。


![image.png](/assets/threejs/c2cd3ce35533e4938b66d5a0f1066d8a458ae4c4.png)


![image.png](/assets/threejs/756055da995bdeb2f28ad8e15d4618d789d3f63c.png)

```javascript
group.add(fish);
fish.position.set(0, -1600, 0);
```
并且调下位置。

看下效果：


![2025-09-05 12.00.43.gif](/assets/threejs/ffa2e55a142825015c795b4d58fce55de2abff44.gif)

这样，鱼就渲染出来了。

现在骨骼动画是自动播放的。

![2025-09-05 12.02.06.gif](/assets/threejs/f9d14ada2357ba26f9a129558ebd1fff2cb05f1e.gif)

我们可以改成通过滚动来控制骨骼动画的播放进度。

首先，我们用 gui 来写一下：

在 animation 对象中可以拿到 duration 动画时长

![image.png](/assets/threejs/6b64c805353bb2080b4b26170e3aa61836a63637.png)

我们通过控制动画的 time 在 duration 范围内变化就可以了：


![image.png](/assets/threejs/a18a33e73d16f7d5603444d5da948af4369270ea.png)

```javascript
clipAction.paused = true;

const gui = new GUI();
gui.add(clipAction, 'time', 0, gltf.animations[0].duration)
```
用 gui 控制 time 变化。

先把场景的旋转和鼠标交互去掉：


![image.png](/assets/threejs/2185a9bc2a87a751a731c3580543ba14ef970528.png)


![image.png](/assets/threejs/512d9e82df8f5ea4d9b796652ecbb8cc73683a9d.png)

改变下 time 看下效果：

![2025-09-05 12.17.14.gif](/assets/threejs/c8359de473ac2ab015a59a21267092381c42a6c8.gif)

我们可以 time 控制动画进程。

那把 gui 的进度条控制，改为滚动控制不就行了？

我们再加一页，方便观察效果：

![image.png](/assets/threejs/2e2ecc077871b622782450514eccf12b3d1ece21.png)

```html
<div className='section section5'>
    <h1>页面尾部</h1>
</div>
```
然后计算滚动的进度来设置 time：


![image.png](/assets/threejs/fb2bb20cef3fdb6fe938198dbe07f185dcb9ce59.png)

```javascript
const pageNo = Math.round(window.scrollY /window.innerHeight);
if(pageNo === 3) {
    const percent = window.scrollY / window.innerHeight - 2.5
    clipAction.time = gltf.animations[0].duration * percent;

    console.log(percent)
}
```

我们是四舍五入的，也就是到了 2.5 屏 就开始做动画，直到 3.5 屏

所以减去 2.5 就是 0 到 1 的范围

看下效果：


![2025-09-05 12.33.21.gif](/assets/threejs/903e910883013b0cd2dfb890b635089765545cfd.gif)

可以看到，进度在 0 到 1 之间变化，鱼的尾巴是左右摆动的。

因为这个骨骼动画运动幅度太小了，不明显。

我们再找一个模型：

用之前那个人物跳舞的模型：

https://github.com/mrdoob/three.js/blob/e9144842962d46f0ab4a7049cc072ad201d9659d/examples/models/gltf/Michelle.glb

![image.png](/assets/threejs/feb3ef103e3a1f8e2b6c9f8331e65e353d8ab5a0.png)

下载下来放到 public 目录下：


![image.png](/assets/threejs/281177c68a5e3759d48eaaa54ce362adeaae8b60.png)

加载下：

创建  dancer.js

```javascript
import gsap from 'gsap';
import * as THREE from 'three';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';

const loader = new GLTFLoader();

const mesh = new THREE.Group();

loader.load("./Michelle.glb", function (gltf) {
    console.log(gltf);
    mesh.add(gltf.scene);

    gltf.scene.scale.setScalar(50);

    const mixer = new THREE.AnimationMixer(gltf.scene);
    const clipAction = mixer.clipAction(gltf.animations[0]);
    clipAction.play();

    const clock = new THREE.Clock();
    function render() {
        requestAnimationFrame(render);

        const delta = clock.getDelta();
        mixer.update(delta);
    }
    render();

})

export default mesh;
```
引入下：

![image.png](/assets/threejs/c75f79f05985151b78bd5b984448425eafdeda86.png)

![image.png](/assets/threejs/7eb26fb09d6bdd431d315b12f68f45f58e8310b9.png)

```javascript
group.add(dancer);
dancer.position.set(0, -2100, 0);
```
再加一屏：


![image.png](/assets/threejs/55a9b150d96b301ea8d1cf1e767009211c9654ca.png)

```html
<div className='section section5'>
    <h1>跳舞ing</h1>
</div>
<div className='section section6'>
    <h1>页面尾部</h1>
</div>
```
最后一屏没有对应的模型，这里加个判断：


![image.png](/assets/threejs/49e0129cc43b55d83c7ff4985d1703f20b53c860.png)

```javascript
if(!mesh.children[pageNo]) {
    return;
}
```

![2025-09-05 12.43.40.gif](/assets/threejs/204654fa8fdabfcca6cde1ee53e8a07d6d02d934.gif)

我们把它暂停，然后通过滚动控制进度：


![image.png](/assets/threejs/af2175c687a831ea1607b65704aaf1bf68b6cfdf.png)

```javascript
const pageNo = Math.round(window.scrollY /window.innerHeight);
if(pageNo === 4) {
    const percent = window.scrollY / window.innerHeight - 3.5
    clipAction.time = gltf.animations[0].duration * percent;
}
```
和之前一样，数值在 3.5 到 4.5 之间变动，控制动画进度。

顺便把模型调大了一点。


![2025-09-05 12.46.22.gif](/assets/threejs/0c3da58c61c2838f5038b046b49dfb5bc514220e.gif)

现在就可以通过上下滚动控制骨骼动画进度了。

把之前注释掉的代码放开，我们看下整体效果：


![2025-09-05 12.47.58.gif](/assets/threejs/974f61fcc394170172d48dcaeb0dc9c351d8112a.gif)


![2025-09-05 12.48.46.gif](/assets/threejs/e8b0cee864e91a36baf12559e10f318c0f160d95.gif)

>案例代码上传了[小册仓库](https://github.com/QuarkGluonPlasma/threejs-course-code/tree/main/3d-webpage)

## 总结

这节我们实现了滚动控制骨骼动画的功能。

可以在滚动的时候让 3D 场景做出一些变化，增加交互性。

不过现在只是一屏来控制一个动画，如果想多屏滚动控制一个动画呢？

下节我们继续探索。

