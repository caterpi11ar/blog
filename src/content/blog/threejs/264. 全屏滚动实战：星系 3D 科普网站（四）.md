---
title: "264. 全屏滚动实战：星系 3D 科普网站（四）"
author: caterpi11ar
pubDatetime: 2026-04-29T00:00:00
featured: false
draft: false
tags: ["Three.js"]
description: "前面把星系和 div 部分都画完了："
---
前面把星系和 div 部分都画完了：

![2025-09-09 10.39.58.gif](/assets/threejs/b1161daf425a20b9a5a9ec7698431a482eb67237.gif)

这节加上滚动的交互：

首先我们监听滚动事件，拿到当前是第几屏：


![image.png](/assets/threejs/e10c5b4f5265f35c46937eb26b09e370c6dc56d1.png)

```javascript
let curPageNo;
window.addEventListener('scroll', () => {
    const pageNo = Math.round(window.scrollY /window.innerHeight);

    if(pageNo !== curPageNo) {
        curPageNo = pageNo;
        console.log(pageNo);
    }
});

export function getCurPageNo() {
    return curPageNo;
}
```

![2025-09-09 11.06.39.gif](/assets/threejs/b0fda928e444b9f1cd4270777059c2066a132a4d.gif)

可以看到，能拿到正确的页码。

然后我们在滚动到每一页的时候，改一下相机位置：

就像前面控制人物行走时一样：

![2025-09-21 21.38.37.gif](/assets/threejs/a89d54042fe4ee8894a2c1a3c28f2bc401a4566c.gif)

把相机加到和星球一个 group 里就行了。

把星球放到一个个 group 里：


![image.png](/assets/threejs/3490f727ee99e41e3bc92575309ebe4a184a19ef.png)

注释掉之前的位置。

把行星放到一个个 group 里。

```javascript
const planetGroup = new THREE.Group();
planetGroup.name = 'planetGroup' + index;
planetGroup.add(planet);
group.add(planetGroup);
```
为什么要注释掉位置呢？

因为有了 group，位置就是相对于局部坐标系了。

我们加个 AxesHelper 看看：

![image.png](/assets/threejs/b1fdb78043ed9a5c2bb803c0e9c22e6f4c09b5de.png)

```javascript
const helper = new THREE.AxesHelper(1000);
planetGroup.add(helper);
```

改变位置的不再是行星，而是整个 group：


![image.png](/assets/threejs/984309b4d8f02d31458505a389927f275e3b29e0.png)

顺便把太阳调小一点：

![image.png](/assets/threejs/773540d052499ebbb448c7ea7777ac3f0c9f31b4.png)

看下效果：


![2025-10-10 18.58.23.gif](/assets/threejs/12ac84cb457cd6fd648149e58e8d06cc37bff85a.gif)

可以看到，每个星球都有一个单独的 group 的局部坐标系。

然后我们把 camera 放到每个 group 里，并调整下位置，滚动切换页码的时候调整下 group：


![image.png](/assets/threejs/af3c4a7f050e5409f7f2ec912358b8a535c60ef1.png)

```javascript
window.addEventListener('scroll', () => {
    const pageNo = Math.round(window.scrollY /window.innerHeight);

    if(pageNo !== curPageNo) {
        curPageNo = pageNo;
        console.log(pageNo);
        if(curPageNo === 0) {
            return;
        }
        camera.parent?.remove(camera);
        const targetGroup = mesh.getObjectByName('planetGroup' + (curPageNo - 1));
        targetGroup.add(camera);

        const planet = targetGroup.children[0];

        const r = planet.geometry.parameters.radius;

        camera.position.set(0, 0, r * 5);
    }
});
```

把 scroll 的处理逻辑拿到 init 函数里，这样可以访问到 camera。

第 0 页是介绍，不用处理，从第 1 开始依次是太阳、水星。。。

拿到对应的 group，把 camera 放里面，并且改一下相机位置在半径 * 5 的位置。

去掉最开始的相机位置：


![image.png](/assets/threejs/93b8965816cdd4e5158982f07b90772453ce5981.png)

看下效果：


![2025-10-10 19.08.03.gif](/assets/threejs/1e7f7c6cae6175b824ab6541b21c482d492de057.gif)

这样，滚动到对应页码的时候，相机就会定位到对应的星球。

去掉所有 AxesHelper，把星轨透明度调高一点，并且点取得更密一点：

![image.png](/assets/threejs/053759cf93e8a60d8d5e89f22f889a0c35251a53.png)

看下效果：


![2025-10-10 19.15.18.gif](/assets/threejs/66703a195a35a0439391bd2bc42c4cf22b3c88b2.gif)

这样，太阳系科普网站就完成了。

>案例代码上传了[小册仓库](https://github.com/QuarkGluonPlasma/threejs-course-code/tree/main/solar-system)

## 总结

这节我们实现了滚动切换星球到视野中央的功能。

滚动的页码计算之前讲过，就是用 window.scrollY 和 window.innerHeight 来计算。

定位星球位置到中央，可以把相机加到星球所在的 group 中，让它跟着 group 一起动。

两者结合，就可以实现滚动页面，切换视野中的行星的功能。

这算是一个综合性的小实战，以后遇到需要滚动控制一些交互效果的需求，就可以用这种方式实现。

