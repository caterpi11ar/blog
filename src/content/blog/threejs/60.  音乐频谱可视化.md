---
title: "60. 音乐频谱可视化"
author: caterpi11ar
pubDatetime: 2026-04-29T00:00:00
featured: false
draft: false
tags: ["Three.js"]
description: "前面学了用 Audio 来播放音乐，音频相关的还有一个 AudioAnalyser 可以用来分析频谱："
---
前面学了用 Audio 来播放音乐，音频相关的还有一个 AudioAnalyser 可以用来分析频谱：

![image.png](/assets/threejs/830fe1e7738551182ca5e0f204c800eeda4c6103.png)

我们知道，声音是由振动产生的，这个振动不是单一频率的振动，而是不同频率的振动的复合。

任何一个声音都可以分析出一个频谱：

![image.png](/assets/threejs/4565fc01592a0ffb003c1b858e8fe976d3a09e87.png)

横轴是频率，纵轴是幅度。

任何一个声音都是不同频率的声音做不同幅度的振动的复合结果。

而用 AudioAnalyser 就可以分析出声音的频谱数据。

那我们就可以用这种频谱数据来实现音频可视化。

![image.png](/assets/threejs/c034042b94c9ca4e9671cf16ae39dc25f9dc5b09.png)

创建项目：

```
npx create-vite audio-analyser
```

![image.png](/assets/threejs/d2f89e5675a44963279d39cea38c877749f5a0b6.png)

进入项目，安装依赖：

```
npm install
npm install --save three
npm install --save-dev @types/three
```

改下 src/main.js

```javascript
import './style.css';
import * as THREE from 'three';
import {
    OrbitControls
} from 'three/addons/controls/OrbitControls.js';

const scene = new THREE.Scene();

const directionLight = new THREE.DirectionalLight(0xffffff, 2);
directionLight.position.set(500, 400, 300);
scene.add(directionLight);

const ambientLight = new THREE.AmbientLight();
scene.add(ambientLight);

const width = window.innerWidth;
const height = window.innerHeight;

const helper = new THREE.AxesHelper(500);
scene.add(helper);

const camera = new THREE.PerspectiveCamera(60, width / height, 0.1, 10000);
camera.position.set(500, 600, 800);
camera.lookAt(0, 0, 0);

const renderer = new THREE.WebGLRenderer();
renderer.setSize(width, height)

function render() {
    renderer.render(scene, camera);
    requestAnimationFrame(render);
}

render();

document.body.append(renderer.domElement);

const controls = new OrbitControls(camera, renderer.domElement);

```
创建 Scene、Light、Camera、Renderer。

改下 style.css
```css
body {
  margin: 0;
}
```
先跑下：

```
npm run dev
```

![image.png](/assets/threejs/e2dbc9445df09cb2204755852d560548bc24c51a.png)

![image.png](/assets/threejs/49795234c7850fbea3b57901e57b88ff7cd481ce.png)

然后加入音频：

把[这个音频](https://github.com/QuarkGluonPlasma/threejs-course-code/blob/main/audio-api/public/superman.mp3)下载下来：

![image.png](/assets/threejs/4cbe79322b69aab10a073990af1119b8e5b7afee.png)

放到 public 目录下：


![image.png](/assets/threejs/5080a1a3f85c980044a3e0104b3c79f6006a0137.png)

然后用 AudioLoader 加载之后，用 Audio 来播放：


![image.png](/assets/threejs/b76fd3738b75517d80b85d3aedc18f75ddb53218.png)
```javascript
const listener = new THREE.AudioListener();
const audio = new THREE.Audio( listener );

const loader = new THREE.AudioLoader();
loader.load('./superman.mp3', function ( buffer ) {
  audio.setBuffer( buffer );
});

document.body.addEventListener('click', () => {
  audio.pause();
  audio.play();
})
```
点击网页的时候播放音乐。

有的时候点击没反应，先暂停再播放就好了。

[jaudio](https://lf-activity-static.juejin.cn/obj/juejin-activity-static/user_book/2788017216685118_1744079378327_1636.mp3)

然后加上 AudioAnalyser 来做分析。


![image.png](/assets/threejs/eb5437c8e6076d51f79b9cb3231c27c9c8b499e4.png)

```javascript
const analyser = new THREE.AudioAnalyser(audio);

function render() {
    const data = analyser.getFrequencyData();
    console.log(data);
    renderer.render(scene, camera);
    requestAnimationFrame(render);
}
```

创建 AudioAnalyser，传入要分析的音频。

在每帧渲染的时候用 getFrequencyData 拿到频谱数据打印下：

![2025-04-14 23.04.01.gif](/assets/threejs/11c02db52f14b012d5f6c6030d0762a14f8868a8.gif)

这个数组有 1024 个元素：

![image.png](/assets/threejs/8d854cc234a59a89381bd160c2f8d83755767e69.png)

我们可以每 50 个算一个平均值

![image.png](/assets/threejs/6c15ad6ae955219f3aa0cce8a9b8b077fbac5755.png)

这样就是 21 组。

然后用立方体画出来就行。

分组用 lodash 的 chunk 方法，安装下：

```
npm install --save lodash-es
```

![image.png](/assets/threejs/a1b60536039ac3a0f4e6429d6908a54d761c0484.png)

![image.png](/assets/threejs/03c4392973c547edca0e0383bf3a1ff4282ae7ba.png)

用 chunk 分组，然后 map 之后用 sum 求和。

```javascript
import _ from 'lodash-es';
```
```javascript
function calc() {
  const frequencyData = analyser.getFrequencyData();

  const sumArr = _.map(_.chunk(frequencyData, 50), (arr) => {
    return _.sum(arr);
  });

  return sumArr;
}

function render() {
    console.log(calc())
    renderer.render(scene, camera);
    requestAnimationFrame(render);
}
```

![image.png](/assets/threejs/2ef4557923d1b1d4db3c115b07b01c582bb17b76.png)

现在打印的就是 21 个元素的数组了。

接下来的事情就简单了，把它作为高度，画 21 个立方体就好了。


![image.png](/assets/threejs/eac8e41fb56ef75d144339645a50cb4a928e49a8.png)

```javascript
const group = new THREE.Group();
for(let i = 0; i < 21; i++) {
  const geometry = new THREE.BoxGeometry(100, 500, 100);
  const material = new THREE.MeshPhongMaterial({
    color: 'orange'
  });
  const mesh = new THREE.Mesh(geometry,material);
  mesh.position.y = 250;
  mesh.position.x = i * 150;
  group.add(mesh);
}
scene.add(group);
```

![2025-04-14 23.37.11.gif](/assets/threejs/78ea64913970cbb2e095f4acf10b7fb3c97e9f81.gif)

我们把这个 group 整体往 x 轴负方向移动一下，然后改下相机位置：



![image.png](/assets/threejs/584245ec4adeb7dd1e947cdc0e683bdaac3c29cb.png)
```javascript
camera.position.set(0, 1000, 2000);
```
```javascript
group.position.x = -1500;
group.position.y = -500;

```
去掉 AxesHelper，看下效果：



![image.png](/assets/threejs/2a24b306c857e09e303781b8a806e64a97d53728.png)

然后每帧计算拿到频谱数据的时候，改变一下立方体的高度：



![image.png](/assets/threejs/5d1d8017e78daa2ee29d1e5b2ac595fcd1c07bb0.png)

```javascript
for(let i = 0; i< group.children.length;i++) {
    const box = group.children[i];
    const height = sumArr[i] / 10;
    box.geometry.dispose();
    box.geometry = new THREE.BoxGeometry(100, height, 100);
    box.position.y = height / 2;
}
```
高度变了，position.y 也得变，这样才能底部和 x 轴平齐。

创建新 BoxGeometry 要把之前的那个的 cpu 资源释放掉，调用 geometry.dispose()

看下效果：


![2025-04-15 00.12.58.gif](/assets/threejs/013c3dd09f9b7ecd271ce757fbb3857092b3a98a.gif)

然后你还可以根据高度来设置不同的顶点颜色，实现高度的区分。

像我们之前做颜色渐变柱状图时那样：

![image.png](/assets/threejs/d3c8b0d02c077e29c80e2f415939d45cbb55aa57.png)

![image.png](/assets/threejs/3a031a55a04bd752844eb1bee79cec4d7cecf504.png)

```javascript
const positions = box.geometry.attributes.position;
const colorsArr = [];
const color1 = new THREE.Color('blue');
const color2 = new THREE.Color('red');
for (let i = 0; i < positions.count; i++) {
    const percent = positions.getY(i) / 300;
    const c = color1.clone().lerp(color2, percent);
    colorsArr.push(c.r, c.g, c.b); 
}
const colors = new Float32Array(colorsArr);
box.geometry.attributes.color = new THREE.BufferAttribute(colors, 3);
```
这里到达 300 高度红色，否则蓝色，根据高度计算一个比例，然后用 color.lerp 计算颜色插值，设置到 geometry.attributes.color

启用顶点颜色需要设置 vertexColors 为 true

![image.png](/assets/threejs/b5a75e73af0cfb43416ae3a05b1c70e7840852a4.png)

看下效果：

![2025-04-15 00.30.06.gif](/assets/threejs/bbfc3857fd282c3bd31d24012f13c1677d3a1f0a.gif)

这样，音乐频谱的可视化就做好了。

这里顺便加一下性能分析的 Stats 工具：


![image.png](/assets/threejs/91c1c2d4bbfbd1c22008649b5f84eadbf45a6e51.png)

```javascript
const stats = new Stats();
document.body.appendChild( stats.domElement );

function render() {
    updateHeight();
    stats.update();
    renderer.render(scene, camera);
    requestAnimationFrame(render);
}
```

![image.png](/assets/threejs/62b7740be519bf10bab78d521689e289a8be0856.png)

每秒 60 帧，很流畅。

案例代码上传了[小册仓库](https://github.com/QuarkGluonPlasma/threejs-course-code/tree/main/audio-analyser)。

## 总结

这节我们实现了音乐频谱可视化的效果。

用 AudioAnalyser 拿到音频频谱数据，然后用 lodash 分组之后求和，最后得到一个 20 多个元素的数组，然后用 BoxGeometry 画立方体来可视化。

每帧修改 box 的高度和 position.y 就好了。

我们还通过自定义顶点颜色实现了根据高度来设置颜色的渐变色效果。

后面用到音频频谱的分析，就可以用 AudioAnalyser 来做。
