---
title: "90. 实战：Three.js Editor（七）"
author: caterpi11ar
pubDatetime: 2026-04-29T00:00:00
featured: false
draft: false
tags: ["Three.js"]
description: "上节我们实现了物体位置的编辑："
---
上节我们实现了物体位置的编辑：

![2025-05-14 06.44.26.gif](/assets/threejs/f2d41a40574b111f3a7d37cfcd063f537183a807.gif)

在 [Three.js Editor](https://threejs.org/editor/) 里还支持旋转、放缩的编辑：


![2025-05-14 07.10.04.gif](/assets/threejs/9382436844ab7aa8a9cef16cf0d67c6cd3644694.gif)

点击左侧的按钮，可以切换不同的编辑模式：

![2025-05-14 07.12.58.gif](/assets/threejs/7d2eeaf1d894496bd96e8cf0f87fddddc9323bdd.gif)

这节我们来实现这些按钮以及对应的编辑功能。

先来写下这些按钮：

![image.png](/assets/threejs/32514a37aa5192162ae5058274d3013f4d54d02b.png)

直接用 antd 的[悬浮按钮](https://ant.design/components/float-button-cn#float-button-demo-group)就行：

![image.png](/assets/threejs/658301eced25a9e5a6a771140597704819a8704b.png)

改下 Main/index.jsx


![image.png](/assets/threejs/06590fc2769299373592e71ac995d500cd282ba6.png)

```javascript
import { FloatButton } from "antd";
import { ArrowsAltOutlined, DragOutlined, RetweetOutlined } from "@ant-design/icons";
```
```javascript
return <div className="Main">
    <div id="threejs-container"></div>
    <FloatButton.Group className="btn-group">
        <FloatButton icon={<DragOutlined />}/>
        <FloatButton icon={<RetweetOutlined />}/>
        <FloatButton icon={<ArrowsAltOutlined />} />
    </FloatButton.Group>
</div>
```
创建三个按钮。

安装下用到的图标库：

```
npm install --save @ant-design/icons
```

改下 App.scss 的样式：


![image.png](/assets/threejs/dce9090d027ae1d03bd36726f13fd5044a38b353.png)

```scss
position: relative;
.btn-group {
  width: 100px;
  height: 200px;
  position:absolute;
  left: 0;
  top:0;
}
```
设置宽、高、绝对定位。

看下效果：


![image.png](/assets/threejs/dfc784a4f6332358affc6a21075ca232052d4c52.png)

接下来就可以实现三种编辑模式了。


![image.png](/assets/threejs/1e0d7f9442196eda48937177f639cbabd9c8952c.png)

这个就是修改 TransformControls 的 mode

点不同的按钮设置不同的 mode 就好了。

我们封装一个方法返回：

![image.png](/assets/threejs/a7e0accf85efb56c3ffa23e90b6ba3a4092402d8.png)

```javascript
function setTransformControlsMode(mode) {
    transformControls.setMode(mode);
}

return {
    scene,
    setTransformControlsMode
}
```

在组件里用 useRef 来保存：


![image.png](/assets/threejs/e6a2d2e9d24843fab940202d33c714a240e660ce.png)

```javascript
const transformControlsModeRef = useRef();
```
```javascript
transformControlsModeRef.current = setTransformControlsMode;
```

点击按钮的时候调用下：


![image.png](/assets/threejs/9cf1b352a7930e3f544550a41e955576d13a862b.png)

```javascript
function setMode(mode) {
    transformControlsModeRef.current(mode);
}

return <div className="Main">
    <div id="threejs-container"></div>
    <FloatButton.Group className="btn-group">
        <FloatButton icon={<DragOutlined />} onClick={() => setMode('translate')}/>
        <FloatButton icon={<RetweetOutlined />} onClick={() => setMode('rotate')}/>
        <FloatButton icon={<ArrowsAltOutlined />} onClick={() => setMode('scale')}/>
    </FloatButton.Group>
</div>
```
试下效果：


![2025-05-14 07.47.30.gif](/assets/threejs/4c97c7de5e66ca33e94cdf5a1f9487f9b735f926.gif)

这样，三种编辑模式的切换就完成了。

但 json 里只保存了位置，并没有保存旋转和缩放的数据：

![image.png](/assets/threejs/e171e5e1aa21c12d9df83a4e577d218f0a989aa8.png)

我们来加一下：

![image.png](/assets/threejs/21144d42f373e01119b898fe2ab2ee0671e25622.png)


![image.png](/assets/threejs/9e3176c3817bb5ef917710e8e67e532a8d68929d.png)

分别在 createBox、createCylinder 里加一下 scale 和 rotation 数据：

```javascript
scale: {
    x: 1,
    y: 1,
    z: 1
},
rotation: {
    x: 0,
    y: 0,
    z: 0
}
```
在渲染的时候设置下：


![image.png](/assets/threejs/e0547f1374f4d96baea7a2dfb94340588197fcbd.png)


![image.png](/assets/threejs/7b4a34656f682f91e3ac11659a1de4180045c64e.png)

```javascript
mesh.scale.copy(scale)
mesh.rotation.x = rotation.x;
mesh.rotation.y = rotation.y;
mesh.rotation.z = rotation.z;
```

然后在用 TransformControls 编辑的时候，把 position、scale、ratation 都更新到 json。

rotation 需要分别设置 x、y、z

把 updatePosition 方法改造下：


![image.png](/assets/threejs/2c35bd08b4116639a6efb987183756d5037b1aa0.png)

根据不同的 type 来分别修改 position、scale、rotation

```javascript
updateMeshInfo(name, info, type) {
    set(state => {
        return {
            data: {
                ...state.data,
                meshArr: state.data.meshArr.map(mesh => {
                    if(mesh.name === name) {
                        if(type === 'position') {
                            mesh.props.position = info;
                        } else if(type === 'scale') {
                            mesh.props.scale = info;
                        } else if(type === 'rotation') {
                            mesh.props.rotation = {
                                x: info.x,
                                y: info.y,
                                z: info.z
                            }
                        }
                    }
                    return mesh;
                })
            }
        }
    })
}
```

改下用的方法：

![image.png](/assets/threejs/6cb319f1222931d414b1ebb8c67f643179371ae5.png)

然后编辑的时候根据 mode 修改不同的数据：


![image.png](/assets/threejs/4b165875363170fae8f0238d5ef3db2eda37d1bc.png)

```javascript
transformControls.addEventListener('change', () => {
    const obj = transformControls.object;
    if(obj) {
        if(transformControls.mode === 'translate') {
            updateMeshInfo(obj.name, obj.position, 'position');
        } else if(transformControls.mode === 'scale') {
            updateMeshInfo(obj.name, obj.scale, 'scale');
        } else if(transformControls.mode === 'rotate'){
            updateMeshInfo(obj.name, obj.rotation, 'rotation');
        }
    }
});
```
试下效果：


![2025-05-14 08.57.44.gif](/assets/threejs/41f44ae12b5b67e1d4439dcd83313d8926844f89.gif)

这样，旋转、缩放的编辑就完成了。

案例代码上传了[小册仓库](https://github.com/QuarkGluonPlasma/threejs-course-code/tree/main/threejs-editor)
## 总结

这节我们实现了旋转、缩放的编辑功能。

原理就是 TransformControls 设置 translate、rotate、scale 三种 mode。

我们加了三个按钮，点击的时候来切换 mode。

并且在编辑的时候根据 mode 来分别把数据同步到 json 中。

这样，位移、旋转、缩放的编辑功能就都完成了。
