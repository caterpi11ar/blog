---
title: "153. 骨骼动画的重定向"
author: caterpi11ar
pubDatetime: 2026-04-29T00:00:00
featured: false
draft: false
tags: ["Three.js"]
description: "3D 场景中可能有好多人物，有的人物模型有跳舞的骨骼动画，有的没有。"
---
3D 场景中可能有好多人物，有的人物模型有跳舞的骨骼动画，有的没有。

如果我想把其中一个模型的骨骼动画应用到其他的模型可以么？

比如这个：

![2025-04-06 22.31.54.gif](/assets/threejs/cc157cd329d081d77a2d55cdfa182c252f165931.gif)

应用到这个：

![2025-09-12 14.07.23.gif](/assets/threejs/740c7bd46aa7c21856ac8069f674562042d540b7.gif)

但是可以的，用骨骼动画的重定向。

创建个项目：

```
npx create-vite animation-retarget-test
```

![image.png](/assets/threejs/6f869ec1f811fb57f768734cdb330e353e4da365.png)

进入项目，安装依赖：

```
pnpm install
pnpm install --save three
pnpm install --save-dev @types/three
```

改下 src/main.js

```javascript
import './style.css';
import * as THREE from 'three';
import {
    OrbitControls
} from 'three/addons/controls/OrbitControls.js';
import mesh from './mesh.js';

const scene = new THREE.Scene();

scene.add(mesh);

const directionLight = new THREE.DirectionalLight(0xffffff, 2);
directionLight.position.set(500, 400, 300);
scene.add(directionLight);

const ambientLight = new THREE.AmbientLight();
scene.add(ambientLight);

const width = window.innerWidth;
const height = window.innerHeight;

const helper = new THREE.AxesHelper(500);
scene.add(helper);

const camera = new THREE.PerspectiveCamera(60, width / height, 0.1, 1000);
camera.position.set(200, 300, 300);
camera.lookAt(0, 0, 0);

const renderer = new THREE.WebGLRenderer();
renderer.setSize(width, height)

function render() {
    renderer.render(scene, camera);
    requestAnimationFrame(render);
}

render();

document.body.append(renderer.domElement);

const controls = new OrbitControls(camera, renderer.domElement);

```
创建 Scene、Light、Camera、Renderer。

改下 style.css
```css
body {
  margin: 0;
}
```
然后创建 mesh.js

```javascript
import * as THREE from 'three';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';

const loader = new GLTFLoader();

const mesh = new THREE.Group();

loader.load("./Soldier.glb", function (gltf) {
    console.log(gltf);
    mesh.add(gltf.scene);
    gltf.scene.scale.set(100, 100, 100);
})

export default mesh;

```
加载士兵模型。


https://github.com/mrdoob/three.js/blob/dev/examples/models/gltf/Soldier.glb

![image.png](/assets/threejs/fced92d10cd82934863b1bcdbf6ac930741a44a8.png)


![image.png](/assets/threejs/916e21c6c8c8f1bf8a536695ac7eb59e6a8955dd.png)


```
npm run dev
```
![image.png](/assets/threejs/ddf066f68c1510439a1cb2197d3358ab80a4e202.png)


![2026-03-15 19.35.34.gif](/assets/threejs/d01cc64be6a15babbb342670584d70c44415c276.gif)

再找一个模型：

下载 three.js 的 [github 仓库](https://github.com/mrdoob/three.js/blob/e3ee9682fb2c776cd77fd8b89f73c321945fa52c/examples/models/gltf/Michelle.glb)里的这个模型：

![image.png](/assets/threejs/f7b12a34b95a12052f57ad22c6f0803bc7eddebc.png)

点击右边的下载按钮即可。

放到项目的 public 目录下：


![image.png](/assets/threejs/b3d98770f88c8de38f4c49a993d947e2cd8c82b5.png)

把这个也加载进来：

创建 mesh2.js

```javascript
import * as THREE from 'three';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';

const loader = new GLTFLoader();

const mesh = new THREE.Group();

loader.load("./Michelle.glb", function (gltf) {
    console.log(gltf);
    mesh.add(gltf.scene);
    gltf.scene.scale.set(100, 100, 100);

    gltf.scene.position.set(200, 0, 0);
})

export default mesh;
```

![image.png](/assets/threejs/67cd4314d618ddf2368ae0f419c564152d00ebf8.png)


![2026-03-15 19.43.14.gif](/assets/threejs/a159d63af9902e24dd2497ceb50b813054e44800.gif)



![image.png](/assets/threejs/f805b94cbea84807c032ce5ac4f3fd0cacceff4a.png)

可以看到，它们分别有不同的动画。

我们播放下：


![image.png](/assets/threejs/35022c368d17c8993d792d0580bc6d9ac7a2ab40.png)

我更新到这里的时候，Clock 的 api 废弃了，用 Timer 的 api

```javascript
const mixer = new THREE.AnimationMixer(gltf.scene);
const clipAction = mixer.clipAction(gltf.animations[0]);
clipAction.play();

const timer = new THREE.Timer();
timer.connect(document);
function render(timestamp) {
    timer.update(timestamp);
    mixer.update(timer.getDelta());
    requestAnimationFrame(render);
}
requestAnimationFrame(render);
```
用 AnimationMixer 播放第一个骨骼动画。


![2026-03-15 19.51.32.gif](/assets/threejs/ded46ea4f160696d0a3141ebf3800d3d0fdfaa46.gif)

然后播放另一个模型的骨骼动画：


![image.png](/assets/threejs/ddebb79e29181f5ee9944b2aa066fadccb76d0b9.png)

```javascript
const mixer = new THREE.AnimationMixer(gltf.scene);
const clipAction = mixer.clipAction(gltf.animations[1]);
clipAction.play();

const timer = new THREE.Timer();
timer.connect(document);
function render(timestamp) {
    timer.update(timestamp);
    mixer.update(timer.getDelta());
    requestAnimationFrame(render);
}
requestAnimationFrame(render);
```


![2026-03-15 19.52.49.gif](/assets/threejs/8a7d6a129c10cca4e7b9973c7eafb7b3f7432805.gif)

那如何把一个模型的骨骼动画应用到另一个呢？

下节继续探索。

>案例代码上传了[小册仓库](https://github.com/QuarkGluonPlasma/threejs-course-code/tree/main/animation-retarget-test)。

## 总结

这节我们加载了两个人物模型，并且播放了他们的骨骼动画。

下节继续探索如果把一个模型的骨骼动画应用到另一个模型。


