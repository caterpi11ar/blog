---
title: "183. 地图飞线（二）"
author: caterpi11ar
pubDatetime: 2026-04-29T00:00:00
featured: false
draft: false
tags: ["Three.js"]
description: "上节画了北京到上海的飞线："
---
上节画了北京到上海的飞线：

![2025-08-17 13.02.20.gif](/assets/threejs/8aaee3d74d3114641a347b66fc6900d3b0f83377.gif)

这节继续把北京到其他城市的飞线画出来：

我们已经拿到每个城市中心点的经纬度了：

![image.png](/assets/threejs/9b2d1729427efaf599375ee3884b853f10832235.png)

遍历画一遍就可以：


![image.png](/assets/threejs/68f8afe5c3c16a598a9ccf83b4bf192a8d540b28.png)

起点都是北京，终点是遍历的每个城市的坐标。


```javascript
const beijingPos = mercator(cityCenterMap.get('北京市'));

for(let [name, center] of cityCenterMap.entries()) {
    if(!name) {
        continue;
    }
    if(name === '北京市') {
        continue;
    }
    const endPos = mercator(center);
    const start = new THREE.Vector3( beijingPos[0], -beijingPos[1], 0 );
    const end  = new THREE.Vector3( endPos[0], -endPos[1], 0 );
    const middle = start.clone().add(end).divideScalar(2);
    middle.z = 100;

    const curve = new THREE.CatmullRomCurve3([
        start,
        middle,
        end
    ]);
    const pointsArr = curve.getPoints(20);

    const geometry = new LineGeometry();
    geometry.setFromPoints(pointsArr);

    const material = new LineMaterial({ 
        color: new THREE.Color('orange'),
        linewidth: 3
    });

    const line = new Line2(geometry, material);
    chinaMap.add(line);
}
```
看下效果：


![2025-08-17 13.18.43.gif](/assets/threejs/1f4ccb0a55e439c5e67d6e1338d650aae85959a1.gif)

这样曲线是画出来了，但比较丑。

不管远近都是一样的高度，这样肯定是不好的。

我们应该根据两个点的距离来设置高度：

![image.png](/assets/threejs/f3e59fdaac181f2e94460342611a2e60e7a5fa68.png)

用两个点的 sub 再取 length 来计算距离，设置到 z

顺便把 lineWidth 调小点。

```javascript
const distance = start.clone().sub(end).length();
middle.z = distance / 3;
```

![2025-08-17 13.23.30.gif](/assets/threejs/512a0a7b859eb7eac098f4ca49c02a665e0ab70b.gif)

这样，离得越近，飞线越低，越远飞线越高。

现在飞线不能标识方向，怎么知道是从北京到其他省市，而不是反过来呢？

所以我们要加一个方向的标识。

先改一下曲线颜色：


![image.png](/assets/threejs/6e567d47ca3f032097a7030c109b2fe36416aa7e.png)

曲线取 100 个点。

然后我们从这 100 个点里取一部分画一条不同颜色的曲线。


![image.png](/assets/threejs/0cad05dfc3edf22104b2efa716b9062ea6c9cb0e.png)

```javascript
const pointsArr2 = pointsArr.slice(30, 30 + 20);

const geometry2 = new LineGeometry();
geometry2.setFromPoints(pointsArr2);

const material2 = new LineMaterial({ 
    color: new THREE.Color('orange'),
    linewidth: 2
});
const line2 = new Line2(geometry2, material2);
chinaMap.add(line2);
```
从第 30 个点开始取 20 个点，画一条橙色的线。


![2025-08-17 13.38.49.gif](/assets/threejs/079e2067f710b0b4d07f0cd24b396b82ec7ae484.gif)

然后让它动起来。

怎么动起来呢？

现在是从 30 开始取 20 个点画的线。

下一帧从 40 开始取 20 个点画线，是不是就动起来了？


![image.png](/assets/threejs/b560683128a890a4b309590c5459cb79cb1b14cc.png)

```javascript
let index = 30;
function render() {
    if(index >= 100) index = 0;
    index++;
    const arr = pointsArr.slice(index, index + 20);
    geometry2.setFromPoints(arr);
    requestAnimationFrame(render);
}

render();
```


![2025-08-17 13.51.21.gif](/assets/threejs/ae2bbff1ac9830a06e0ba690321052172ec60bf6.gif)

有点慢，我们还是用 gsap 来控制：

```
pnpm install --save gsap
```

![image.png](/assets/threejs/e4383523de6792c8f0a5a236c9025caa0c77f294.png)

```javascript
let obj = { index: 0 };
gsap.to(obj, {
    index: 100,
    ease: 'none',
    repeat: -1,
    duration: 2,
    onUpdate() {
        const arr = pointsArr.slice(obj.index, obj.index + 20);
        geometry2.setFromPoints(arr);
    }
})
```


![2025-08-17 13.55.13.gif](/assets/threejs/bedc77328918b1585be0e6ea041578457ef12cc9.gif)

现在就是 2s 一次飞线动画了。

我们换一个先加速后减速的缓动动画：


![image.png](/assets/threejs/5919dac3f677e9e0701e8dc34251a9ac4b293b01.png)

去掉 AxesHelper 看下效果：

![2025-08-17 13.57.22.gif](/assets/threejs/a94dcfebe951f766dfb3cc473e551d60c6e1382f.gif)

这样比匀速好很多，更有方向感。


![2025-08-17 14.24.17.gif](/assets/threejs/7034c8f850bfb4fe846d65176aff0f45901b8fb7.gif)

>案例代码上传了[小册仓库](https://github.com/QuarkGluonPlasma/threejs-course-code/tree/main/map-flyline)

## 总结

这节我们实现了飞线动画。

从曲线上取了一部分的点，又画了一条不同颜色的新的曲线。

然后用 gasp 做动画，不断改变起始点的 index，从原曲线上截取不同的位置的点来画新曲线。

这样就可以实现标识方向的动画。
