---
title: "190. 实战：交互式地球仪（四）"
author: caterpi11ar
pubDatetime: 2026-04-29T00:00:00
featured: false
draft: false
tags: ["Three.js"]
description: "上节实现了国家列表和搜索："
---
上节实现了国家列表和搜索：

![2025-08-24 17.18.10.gif](/assets/threejs/96dc1afe3ca4e51cca3d22b5cd116e368e995f74.gif)

这节根据点击的国家名在地图中定位到对应国家。

首先我们暴露一个方法：

![image.png](/assets/threejs/3d070e3e17a23bad44cc5eef77c99ab9a88ce60a.png)

```javascript
function focusCountry(name) {
    console.log('focus', name);
}
```
在组件里接收下：


![image.png](/assets/threejs/660b3576eace6d4ec3e0b7436b2fc2c1b70d3816.png)

```javascript
const focusCountryRef = useRef(() => {});
```
```javascript
focusCountryRef.current = focusCountry;
```
点击国家名的时候调用：


![image.png](/assets/threejs/dc912e84c6f63728c0d7860b5d4b5af29c5bb418.png)

```javascript
onClick={() => {
    focusCountryRef.current(item);
}}
```
测试下先：


![2025-08-24 22.31.14.gif](/assets/threejs/8765298ee38ee46235cca3253b22bd6624173548.gif)

然后我们给国家的轮廓多边形、label 加一个 name：


![image.png](/assets/threejs/55f2b5866a9a1da91eb901a07350595ca6556f8c.png)

```javascript
province.name = 'shape' + feature.properties.name_zh;
```
```javascript
label.name = 'label' + feature.properties.name_zh;
```
这样就可以根据 name 来查找国家对应的轮廓、标签对象了：


![image.png](/assets/threejs/48854ca17efff955006092414b18cd69eb2b5014.png)

```javascript
function focusCountry(name) {
    const focusLabel = scene.getObjectByName('label' + name);

    const dir = focusLabel.position.clone().normalize();
    const newCameraPos = focusLabel.position.clone().add(dir.multiplyScalar(300));
    camera.position.copy(newCameraPos);
}
```
我们查找到国家的标签对象。

它的 position 向量作为方向，先转为单位向量，乘以 300，就是在这个向量的方向上平移 300

把它设为相机位置。

看下效果：


![2025-08-24 23.22.43.gif](/assets/threejs/202ab2fb652abefd0ba9bd74724df2e764e59b53.gif)

可以看到，位置都是对的。

我们把选中的国家的国家名变大一点：


![image.png](/assets/threejs/ae79f93d7111d74394ff9bb88654dcdc5f0cbff8.png)

```javascript
focusLabel.textHeight = 20;

const focusShape = scene.getObjectByName('shape' + name);

focusShape.traverse((obj) => {
    if(obj.isLine2) {
        obj.material.color.set('red');
        obj.position.add(dir.clone().normalize().multiplyScalar(2));
    }
})
```
把国家名字变大，并且把国家边界线变为红色。

这里为了让国家边界线沿着 label 的方向移动一段距离，避免和其他的线重合。

看下效果：



![2025-08-24 23.28.55.gif](/assets/threejs/337787923b7659f1c1e46de9a455ac5fdbfebedf.gif)

有点太近了，远一点：


![image.png](/assets/threejs/1767bf2be604e4bb941b2ff2adecf0707279db5d.png)


![2025-08-24 23.30.17.gif](/assets/threejs/234c817f5fe70739f4c2b80048787dd23d1fa320.gif)

然后当选中另一个国家的时候，之前的选中效果要去掉：


![image.png](/assets/threejs/8562559324e774817e9e76d9a0b4230e669d5943.png)


![image.png](/assets/threejs/3867d1f28c52f4f85dd5a93a91e0170bc276f86b.png)

```javascript
let prevLabel;
let prevShape;
function focusCountry(name) {
    if(prevLabel) {
        prevLabel.textHeight = 3;
    }
    if(prevShape) {
        const dir = prevLabel.position.clone().normalize();

        prevShape.traverse((obj) => {
            if(obj.isLine2) {
                obj.material.color.set('gold');
                obj.position.sub(dir.clone().normalize().multiplyScalar(2));
            }
        })
    }
    const focusLabel = scene.getObjectByName('label' + name);

    const dir = focusLabel.position.clone().normalize();
    const newCameraPos = focusLabel.position.clone().add(dir.multiplyScalar(600));
    camera.position.copy(newCameraPos);

    focusLabel.textHeight = 20;

    const focusShape = scene.getObjectByName('shape' + name);

    focusShape.traverse((obj) => {
        if(obj.isLine2) {
            obj.material.color.set('red');
            obj.position.add(dir.clone().normalize().multiplyScalar(2));
        }
    })
    prevLabel = focusLabel;
    prevShape = focusShape;
}
```
就是恢复之前的数据。

试一下：


![2025-08-24 23.36.34.gif](/assets/threejs/6842822d2938ec5524ef061944dbac4f948e1d37.gif)

这个相机位置的变化我们用 gsap 来做缓动动画：


![image.png](/assets/threejs/ee142d737d4f85f6b4253941c762fdd21e38835b.png)

```javascript
gsap.to(camera.position, {
    ...newCameraPos,
    ease: 'none',
    repeat: 0,
    duration: 1
});
```

![2025-08-24 23.41.58.gif](/assets/threejs/cfd858cb3d0261e0893ec2e174f58d30ca0eea9c.gif)


![2025-08-24 23.42.25.gif](/assets/threejs/a86f2bb981a7176417d6ea62a6528872e9f03eb0.gif)

这样，国家的定位就完成了。

>案例代码上传了[小册仓库](https://github.com/QuarkGluonPlasma/threejs-course-code/tree/main/interactive-globe)

## 总结

这节我们实现了点击国家名定位到对应国家的功能。

定位对应国家时，我们通过 label 的位置单位向量化作为方向，让相机定位到这个方向的某个位置。

然后改一下轮廓的颜色、label 的字体大小。

当选中别的国家时，把之前的恢复原状。

这样我们的交互式地球仪就完成了。
