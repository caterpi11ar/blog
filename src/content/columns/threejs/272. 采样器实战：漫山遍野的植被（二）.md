---
title: "272. 采样器实战：漫山遍野的植被（二）"
author: caterpi11ar
pubDatetime: 2026-04-29T00:00:00
featured: false
draft: false
tags: ["Three.js"]
description: "上节把山坡画了出来："
---
上节把山坡画了出来：

![2025-10-26 19.14.08.gif](/assets/threejs/13e9336c46f57c623faaf37a25bf28807fd03d6e.gif)

这节加上植被。

我们用表面采样器在山坡上随机取一些点：


![image.png](/assets/threejs/377f098bcb131386ea3cffd1db9c3f5e80e431c1.png)

```javascript
const sampler = new MeshSurfaceSampler(mountainside).build();

const flowerPositions = [];
const position = new THREE.Vector3();

// 在地形表面采样3000个点作为鲜花位置
for (let i = 0; i < 3000; i++) {
    sampler.sample(position);
    flowerPositions.push(position.x, position.y, position.z);
}


const flowerGeometry = new THREE.BoxGeometry(2, 2, 30);
const flowerMaterial = new THREE.MeshPhongMaterial({
    color: 'pink'
});

const flowersGroup = new THREE.Group();

for (let i = 0; i < flowerPositions.length; i += 3) {
    const flower = new THREE.Mesh(flowerGeometry, flowerMaterial);
    const flowerPosition = new THREE.Vector3(
        flowerPositions[i], 
        flowerPositions[i + 1], 
        flowerPositions[i + 2]
    );
    
    flower.position.copy(flowerPosition);
    
    flowersGroup.add(flower);
}

mountainside.add(flowersGroup);
```
我们采样了 3000 个点，然后画了立方体。

看下效果：


![2025-10-26 19.29.00.gif](/assets/threejs/ee91266a956c8c12b0c3b84ea809978cc7f80d37.gif)

用采样器可比我们随机取简单多了。

然后找个花的模型：

https://sketchfab.com/3d-models/garden-flower-vegetation-683e2043a99d4bddb750098a934e3533


![image.png](/assets/threejs/c0d491895e4d08ec6e53250b378664ee0545136a.png)

下载 glb 放到 public 目录：


![image.png](/assets/threejs/74fc85a065dcb815a6b5cbd92328cc474b239574.png)

引入下：


![image.png](/assets/threejs/b4c7ad079eabccbb9bd9c4bd7e071c53b94a3ebb.png)

```javascript
const loader = new GLTFLoader();
const flowersGroup = new THREE.Group();

loader.load('/flower.glb', (gltf) => {
    const flowerModel = gltf.scene;

    const sampler = new MeshSurfaceSampler(mountainside).build();
    
    const flowerPositions = [];
    const position = new THREE.Vector3();
    
    for (let i = 0; i < 500; i++) {
        sampler.sample(position);
        flowerPositions.push(position.x, position.y, position.z);
    }
    
    // 在每个采样点放置鲜花模型
    for (let i = 0; i < flowerPositions.length; i += 3) {
        const flower = flowerModel.clone();
        const flowerPosition = new THREE.Vector3(
            flowerPositions[i], 
            flowerPositions[i + 1], 
            flowerPositions[i + 2]
        );
        
        flower.position.copy(flowerPosition);
        
        flower.scale.setScalar(3);

        flower.rotateX(Math.PI / 2);
        // 启用视锥剔除优化
        flower.frustumCulled = true;
        
        flowersGroup.add(flower);
    }
    
    mountainside.add(flowersGroup);
});
```

这样代码比较重要：


![image.png](/assets/threejs/67e33385f9558f0691febc1f04f80a6c2f2d2dd5.png)

它是只渲染看到的模型，视椎体内看不到的不渲染。


![2025-10-26 19.50.52.gif](/assets/threejs/1c06631646579cee20ec29adc7a6d4ba1ab7401b.gif)

只有花还是太突兀了，我们再加一些树。

https://sketchfab.com/3d-models/pine-tree-e52769d653cd4e52a4acff3041961e65


![image.png](/assets/threejs/dea62b7ac3698ae189acf75bdc090cb7e8c03c06.png)


![image.png](/assets/threejs/ba7fdc5bf6a03c56836da801d1e9634483c15761.png)

引入下：

![image.png](/assets/threejs/a3ccdafd7f5649f7fed277018b9335f6bffac7fa.png)

```javascript
// 加载树木模型
loader.load('/tree.glb', (gltf) => {
    const treeModel = gltf.scene;

    const sampler = new MeshSurfaceSampler(mountainside).build();
    
    const treePositions = [];
    const position = new THREE.Vector3();
    
    // 生成树木位置，数量比鲜花少一些
    for (let i = 0; i < 200; i++) {
        sampler.sample(position);
        treePositions.push(position.x, position.y, position.z);
    }
    
    // 在每个采样点放置树木模型
    for (let i = 0; i < treePositions.length; i += 3) {
        const tree = treeModel.clone();
        const treePosition = new THREE.Vector3(
            treePositions[i], 
            treePositions[i + 1], 
            treePositions[i + 2]
        );
        
        tree.position.copy(treePosition);
        
        tree.scale.setScalar(0.2);

        tree.rotateX(Math.PI / 2);
        
        // 添加随机的Y轴旋转（绕垂直轴旋转）
        const randomRotationY = Math.random() * Math.PI * 2; // 0 到 2π 的随机旋转
        tree.rotateY(randomRotationY);
        
        // 启用视锥剔除优化
        tree.frustumCulled = true;
        
        treesGroup.add(tree);
    }
    
    mountainside.add(treesGroup);
});
```

![2025-10-26 19.58.24.gif](/assets/threejs/fa83c80eed1c8ccf3bec4b0cebc428612421d773.gif)

这样好多了。

现在山的颜色有点奇怪，我们改一下。

自己找个网页用调色板调好复制出来就行：

![image.png](/assets/threejs/2a64171dd8df247e5d918e4611ce386eaeab1e8c.png)


![image.png](/assets/threejs/6c739356227d234ed26cad50dacdc96733c0d08f.png)

```javascript
const color1 = new THREE.Color('#efcc98');  // 低处 
const color2 = new THREE.Color('#70624F');  // 中低处 
const color3 = new THREE.Color('#C0A068');  // 中高处
const color4 = new THREE.Color('#eeb766');  // 高处 
```

灯光也调一下，去掉环境光，平行光亮度调低：

![image.png](/assets/threejs/2e3d1290cd23f85fc2b23c8b4b01e7d07021b015.png)

```javascript
const light = new THREE.DirectionalLight(0xffffff, 0.5);
light.position.set(500, 3000, 600);
scene.add(light);
```
顺便去掉坐标轴。


![2025-10-26 20.18.00.gif](/assets/threejs/c0a0f1c3fc3b936e7eae88b828a406ebfec11cd2.gif)

这样就好多了。

>案例代码上传了[小册仓库](https://github.com/QuarkGluonPlasma/threejs-course-code/tree/main/hillside-flowers)

## 总结

这节我们给山坡加上了树和鲜花。

用的是采样器实现的，这也是这个案例的主要练习的东西。

以后遇到这种需要在物体表面随机加一些东西的时候，就可以用表面采样器来做。

