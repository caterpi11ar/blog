---
title: "67. HDR：亮度范围更广的全景图"
author: caterpi11ar
pubDatetime: 2026-04-29T00:00:00
featured: false
draft: false
tags: ["Three.js"]
description: "前面我们设置过好几次天空盒背景了。"
---
前面我们设置过好几次天空盒背景了。

我们是从网上找了一些全景图：

![image.png](/assets/threejs/2cfad48bf285af2461f48e7331dd22271b392175.png)

用工具分割成前后左右上下 6 张图片：

![image.png](/assets/threejs/303a7776b3b5813d63922cedb350a0b0c8c2d911.png)

![image.png](/assets/threejs/0bc6ea8a579fc0d38cae1a967727e4ede7003e92.png)

![image.png](/assets/threejs/1b31da40895d71e41a63e9059f70099090a9a782.png)

然后用 CubeTextureLoader 加载并在代码里使用的：


![image.png](/assets/threejs/9e2025ba6d5b353fbf1f19f6d21f1ebab71d951c.png)

但其实设置全景图还有另一种方式，就是 HDR 文件。

![image.png](/assets/threejs/d18ab0d36aefd2433bdc5535d0f651200e6009d8.png)

它比 png、jpg 能存储更大的亮度范围，很适合用来做全景图。

比如把 threejs 官方仓库的这个 hdr 文件下载下来：

https://github.com/mrdoob/three.js/blob/b2e0b0ce5cd1aee526a93c60b08ea5510e33eda0/examples/textures/equirectangular/san_giuseppe_bridge_2k.hdr

![image.png](/assets/threejs/219d57213d02c42be3480db7943a5ad198994a0c.png)

![image.png](/assets/threejs/fb71afda2abec5615eba71457ab679fc6a66b538.png)

这明显也是一张全景图：

![image.png](/assets/threejs/20eda34c720b07ae317f04fbcb43e4c4f7ee925f.png)

如何加载 hdr 文件作为全景图呢？

这就需要用到 RGBELoader 了。

我们来写下代码：

```
npx create-vite hdr-background
```

![image.png](/assets/threejs/c5852a9b23764b9fd03b5e430b9c70809b433ed9.png)

进入项目，安装依赖：

```
npm install
npm install --save three
npm install --save-dev @types/three
```

改下 src/main.js

```javascript
import './style.css';
import * as THREE from 'three';
import {
    OrbitControls
} from 'three/addons/controls/OrbitControls.js';
import mesh from './mesh.js';

const scene = new THREE.Scene();
scene.add(mesh);

const directionLight = new THREE.DirectionalLight(0xffffff);
directionLight.position.set(500, 400, 300);
scene.add(directionLight);

const ambientLight = new THREE.AmbientLight();
scene.add(ambientLight);

const width = window.innerWidth;
const height = window.innerHeight;

const helper = new THREE.AxesHelper(500);
scene.add(helper);

const camera = new THREE.PerspectiveCamera(60, width / height, 0.1, 10000);
camera.position.set(300, 700, 300);
camera.lookAt(0, 0, 0);

const renderer = new THREE.WebGLRenderer({
  antialias: true
});
renderer.setSize(width, height)

function render(time) {
    renderer.render(scene, camera);
    requestAnimationFrame(render);
}

render();

document.body.append(renderer.domElement);

const controls = new OrbitControls(camera, renderer.domElement);

window.onresize = function () {
  const width = window.innerWidth;
  const height = window.innerHeight;

  renderer.setSize(width,height);

  camera.aspect = width / height;
  camera.updateProjectionMatrix();
};
```
创建 Scene、Light、Camera、Renderer。

改下 style.css

```css
body {
  margin: 0;
}
```
先跑下：

```
npm run dev
```

![image.png](/assets/threejs/e2dbc9445df09cb2204755852d560548bc24c51a.png)


![image.png](/assets/threejs/2a587348b4f710b934c4e6d736f6d64cd884e91c.png)

然后我们加载下 hdr 文件：

把刚才的 pbr 文件复制到 public 目录下：

![image.png](/assets/threejs/8f97253651abcf800db2618c38a66e6ef5a61bd9.png)

代码里加载下：


![image.png](/assets/threejs/03035938921923c665f0d421a932722f4e51ba04.png)

```javascript
const rgbeloader = new RGBELoader();

rgbeloader.load('./pic.hdr', function ( texture ) {
  texture.mapping = THREE.EquirectangularReflectionMapping;
  scene.background = texture;
});
```

![2025-04-18 22.45.55.gif](/assets/threejs/3f32dd3233fb6cad2f3f6e0bfacae0efa77ec6d9.gif)

可以看到，现在的光线非常真实，相比之前用 CubeTextureLoader 加载 6 张图作为。

这就是 hdr 文件的好处。

这个 mapping 是指定如何解析这张图片的：


![image.png](/assets/threejs/96d6809a5cd911aaf11a8d8b463e15b98859d461.png)

不指定就是这样：

![image.png](/assets/threejs/a346a95ae38f09698337912858764d5a3cc52718.png)

其实之前的全景图也不是非得切割成天空盒的六张图再用，比如这张图片：


![pic.jpg](/assets/threejs/782201075f4249bcf2c97628fa7a1bbe6f68a14f.jpg)

![image.png](/assets/threejs/d69e3ff394d1da4b5163559987a2b5d2a0187c6f.png)

如果不切割成 6 张图，也可以直接用：


![image.png](/assets/threejs/671af5fe8d9b3e25afda6a5263cb8259b684092d.png)

```javascript
const textureLoader = new THREE.TextureLoader();

textureLoader.load('./pic.jpg', function ( texture ) {
  texture.mapping = THREE.EquirectangularReflectionMapping;
  scene.background = texture;
});
```

![2025-04-18 22.54.35.gif](/assets/threejs/40f67bed9c349bca9e3e4fd01d9fe825da98184f.gif)

jpg 文件是没有 hdr 文件存储的亮度信息多的，全景图还是用 hdr 比较好。

案例代码上传了[小册仓库](https://github.com/QuarkGluonPlasma/threejs-course-code/tree/main/hdr-background)。

## 总结

这节我们学了用 .hdr 文件作为全景图。

hdr 文件能够存储范围更广的亮度信息，作为全景图更加真实。

用 RGBELoader 加载之后，设置 mapping 为 EquirectangularReflectionMapping，然后作为 scene.background 就可以了。

当然，普通的 jpg 文件也不用分割成 6 张图后再用 CubeTextureLoader 加载，也可以用这种方式运行时分割。

后面做全景图的时候，尽量用 hdr 文件，会更加真实。
